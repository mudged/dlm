# Design

Architecture and design of the application.

- [`architecture.md`](architecture.md) — the authoritative technical design: Go service runtime,
  Next.js + Tailwind frontend, UI ↔ API coordination, Raspberry Pi deployment, system boundaries and
  request flows (Mermaid diagrams), and baseline security notes.

The document uses internal section references (e.g. `§3.18`, `§8.24`); keep the section numbering
stable when editing so those cross-references stay valid. Requirements this design realizes live in
[`../requirements/`](../requirements).
