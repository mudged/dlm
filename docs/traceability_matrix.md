# Traceability matrix

Automated and manual verification recorded by the `@verifier` agent. Column order matches `docs/templates/traceability-matrix-template.md`.

| ID | Functional Requirement | System Component | Test Case Number | Status | Verification |
|----|------------------------|------------------|------------------|--------|--------------|
| TRC-001 | REQ-001 — Go service and Next.js + Tailwind web UI | `backend/cmd/server`, `backend/internal/httpapi`, `backend/internal/config`, `backend/internal/webdist` | `go test ./...` (`internal/httpapi/handlers_test.go`, `internal/config/config_test.go`, `internal/acceptance/requirements_test.go`) | Pass | 2026-03-28: `cd backend && go test ./... -count=1` — all packages ok. |
| TRC-002 | REQ-001 — Go service and Next.js + Tailwind web UI | `web/app/*`, `web/components/*`, `web/next.config.ts`, `scripts/copy-web-export.mjs` | `npm run lint`; `npm run build` (static export to `web/out/`) | Pass | 2026-03-28: `next lint` clean; `next build` with `output: "export"` completed. |
| TRC-003 | REQ-001 — Go service and Next.js + Tailwind web UI | `docs/requirements.md` REQ-001; `docs/acceptance_criteria.md` Feature "Full-stack composition" | AC Gherkin REQ-001 scenarios; `TestAcceptance_REQ001_fullStackComposition` | Pass | 2026-03-28: Doc audit + automated string checks in `internal/acceptance`. |
| TRC-004 | REQ-002 — Responsive, reactive UI | `web/components/ui/Button.tsx`, `StatusRefresh.tsx`, `StatusOnMount.tsx`, `web/app/page.tsx` | AC REQ-002 + `TestAcceptance_REQ002_responsiveAndReactive` | Pass | 2026-03-28: Client `fetch`; touch targets; responsive layout; requirements text checks automated. |
| TRC-005 | REQ-003 — Raspberry Pi 4 Model B | `docs/architecture.md` §1, §3.4, §6; `README.md` | AC REQ-003 scenarios; `TestAcceptance_REQ003_raspberryPiDeployment` | Pass | 2026-03-28: Architecture + README document linux/arm64, Pi 4B, RAM/CPU, single process; requirements checks automated. |
| TRC-006 | REQ-004 — Single executable packaging | `backend/internal/webdist/embed.go`, `backend/cmd/server/main.go`, `backend/internal/httpapi/router.go`, `README.md` | Implementation review vs `docs/architecture.md` §3.4–§3.5, §5–§6 | Pass | 2026-03-28: Single binary embeds UI FS + API; `npm run release:sync` bake step; README documents cross-compile. |
| TRC-007 | REQ-004 — Single executable packaging | `web/next.config.ts`, `web/package.json` | REQ-004 open question (SSR vs static): arch § "Architectural resolution" | Pass | 2026-03-28: `output: "export"`; static export + client hydration; Node at build time only. |
| TRC-008 | REQ-004 + AC | `docs/requirements.md` REQ-004; `docs/acceptance_criteria.md` Feature "Single executable packaging without Docker" | AC Gherkin REQ-004 scenarios; `TestAcceptance_REQ004_singleBinaryNoDocker` | Pass | 2026-03-28: Doc audit + automated requirements text checks. |
| TRC-009 | REQ-001–REQ-017 (acceptance ↔ requirements text) | `backend/internal/acceptance/requirements_test.go` | `go test ./internal/acceptance -count=1` (all `TestAcceptance_*`) | Pass | 2026-04-03: **`TestAcceptance_REQ001`–`REQ007`** plus **`REQ008`**, **`REQ009`**, **`REQ010`** (see **TRC-024**). **Updated 2026-04-06:** suite extended by **`TestAcceptance_REQ011_restLightStateAPI`**, **`TestAcceptance_REQ012_visualizationReflectsLightState`**, **`TestAcceptance_REQ013_modelViewPaginationAndBulkSettings`** (**TRC-029** / **TRC-031** / **TRC-033**), **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`**, **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`** (**TRC-043** / **TRC-044**). **`### REQ-015`** still has no dedicated `TestAcceptance_REQ015_*` — **TRC-040** / **TRC-042** remain the gate for scenes requirements text. **Updated 2026-04-05:** **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`** covers **REQ-022** text + **`web/package.json`** + editor source (**TRC-056**). |
| TRC-010 | REQ-001, REQ-004, architecture §3.2 (static export + SPA fallback) | `backend/internal/httpapi/router.go` (`staticExportHandler`) | `TestStatic_unknownClientRoute_fallsBackToIndexHTML`, `TestStatic_missingNextAsset_returnsNotFound`, `TestStatic_servesEmbeddableExport`, `TestAPI_precedenceOverStaticPrefix` | Pass | 2026-03-28: Extensionless paths serve `index.html` via `ServeFileFS`; missing `/_next/*` 404; API not swallowed by static handler. |
| TRC-011 | REQ-005 — Wire light model (data shape, CSV, metadata) | `backend/internal/wiremodel/csv.go`; `backend/internal/store/store.go` (schema, `LightDTO` / float coords) | `go test ./internal/wiremodel -count=1`; `go test ./internal/store -count=1` (`TestStore_CreateListGetDelete`, `TestStore_EmptyLights`) | Pass | 2026-04-03: CSV header `id,x,y,z`; ≤1000 rows; contiguous ids 0…n−1; empty model (header-only) allowed; metadata `name` + server `created_at` in DB. |
| TRC-012 | REQ-006 — List, view, delete, create via CSV upload | `backend/internal/httpapi/models.go`, `router.go`; `backend/cmd/server/main.go`; `backend/internal/store` | `TestModels_createValidCSV`, `TestModels_listGetDelete`, `TestModels_duplicateNameConflict` in `internal/httpapi/models_test.go` | Pass | 2026-04-03: `GET/POST /api/v1/models`, `GET/DELETE /api/v1/models/{id}`; multipart `name`+`file`; 201/204/404/409 behaviors exercised. |
| TRC-013 | REQ-006 — List, view, delete, create (UI) | `web/app/models/page.tsx`, `web/app/models/new/page.tsx`, `web/app/models/detail/*`, `web/app/page.tsx` | `npm run lint`; `npm run build` (`output: "export"`); routes `/models`, `/models/new`, `/models/detail` | Pass | 2026-04-03: Client `fetch` to same-origin `/api/v1/models…`; responsive layout patterns; **Note:** detail uses query `?id=` (static export) rather than path `/models/{id}` — equivalent to §4.6 intent. |
| TRC-014 | REQ-007 — Validate CSV on model upload | `backend/internal/wiremodel` (parse/validate); `backend/internal/httpapi/models.go` (`createModel`, `MaxBytesReader`, error envelope) | `TestParseLightsCSV_*` (`csv_test.go`); `TestModels_rejectBadIds`, `TestModels_rejectWrongHeader` (`models_test.go`) | Pass | 2026-04-03: Wrong header, non-contiguous ids, non-numeric coords, >1000 rows → 400 + `validation_failed`; duplicate name → 409; create uses DB transaction (no partial model on failure). |
| TRC-015 | REQ-005–REQ-007 — Config and persistence per architecture §3.3 | `backend/internal/config/config.go` (`DLM_DB_PATH`, `DLM_DATA_DIR`); `store.Open` + `modernc.org/sqlite` | `TestLoad_defaults`, `TestLoad_dlmDBPath` (`config_test.go`); `TestStore_DuplicateName` | Pass | 2026-04-03: Default `data/dlm.db`; SQLite file is runtime state (not embedded); UNIQUE on `models.name`. |
| TRC-016 | REQ-001–REQ-009 — Regression suite (backend) | Full backend + acceptance doc checks | `cd backend && go test ./... -count=1`; `go build ./cmd/server` | Pass | 2026-04-03: `go test ./... -count=1`; `go build -o /tmp/dlm-verify ./cmd/server` — all packages green (post–§3.8 face/surface-even samples). |
| TRC-017 | REQ-001–REQ-017 — Requirements doc string checks (`requirementBlock`) | `backend/internal/acceptance/requirements_test.go` | `go test ./internal/acceptance -count=1`; `TestAcceptance_REQ001` … `TestAcceptance_REQ010`; **`TestAcceptance_REQ011_restLightStateAPI`**; **`TestAcceptance_REQ012_visualizationReflectsLightState`**; **`TestAcceptance_REQ013_modelViewPaginationAndBulkSettings`**; **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`**; **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`**; **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`** | Pass | 2026-04-03: REQ-001–010 each have a `TestAcceptance_REQxxx_*` reading **`docs/requirements.md`** (**REQ-008** also reads **`README.md`**). **Updated 2026-04-06:** **TRC-029** / **TRC-033** pattern extended to **REQ-016** / **REQ-017** (**TRC-043** / **TRC-044**). **REQ-015** still lacks `TestAcceptance_REQ015_*` — see **TRC-040** optional gap. **Updated 2026-04-05:** **REQ-022** uses **`requirementBlock`** + **`web/package.json`** + **`PythonRoutineEditorClient.tsx`** checks in **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`** (**TRC-056**). |
| TRC-018 | REQ-005 — Acceptance (requirements block) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ005_wireLightModel` | Pass | 2026-04-03: `requirementBlock` scopes ### REQ-005; asserts 1000 cap, CSV columns, metadata, sequential indices. |
| TRC-019 | REQ-006 — Acceptance (requirements block) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ006_modelCRUDAndCSVUpload` | Pass | 2026-04-03: Asserts list/view/delete/upload/CSV and mobile/tablet/desktop UX notes. |
| TRC-020 | REQ-007 — Acceptance (requirements block) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ007_validateCSVOnUpload` | Pass | 2026-04-03: Asserts validation, sequential ids, 1000 cap, finite coords, actionable feedback, no partial persist. |
| TRC-021 | REQ-008 — Single command to build and run locally | `scripts/run.sh`; `README.md` (exact `./scripts/run.sh`, `DLM_SKIP_NPM_CI`); `AGENTS.md` | Implementation review vs `docs/architecture.md` §3.7; `go test ./... -count=1` (no regression) | Pass | 2026-04-03: Script uses `set -euo pipefail`, `web/` then `npm ci` + `release:sync`, `backend/` + `exec go run ./cmd/server`; README documents invocation and Windows note. |
| TRC-022 | REQ-009 — Default sample models (sphere, cube, cone) | `backend/internal/samples/*` — **`geom.go`** chord helpers; **`cube.go`** six **face serpentines** + **`appendCubeBridgePoints`** axis-aligned **surface** bridges; **`sphere.go`** **Fibonacci** sphere + greedy **NN** order + **`subdivideGreatCircleArc`**; **`cone.go`** **area-split** **`coneBaseSpiral`** + **`coneRingAtZ`** lateral stack + ruling to **apex** + **`trimConeChain`**; **`store.SeedDefaultSamples`**; **`README.md`** (Default samples) | `TestSphereLights_surfaceChordCount`, `TestCubeLights_surfaceChordCount`, `TestConeLights_surfaceChordCount`; `TestStore_SeedDefaultSamples_idempotent` | Pass | 2026-04-03: `go test ./internal/samples -count=1` — **500 ≤ n ≤ 1000** per shape; chord band ± **`SpatialTestTol`**; sphere on **‖p‖=R** + hemisphere bound; cube **six faces** + **85%** loose-interior quota; cone **lateral + base**; store seed idempotent. |
| TRC-023 | REQ-010 — Three.js visualization when viewing a model | `web/package.json` (direct `three`); `web/components/ModelLightsCanvas.tsx`; `web/lib/wireSegments.ts`, `web/lib/lightBounds.ts`; `web/app/models/detail/ModelDetailClient.tsx` (`next/dynamic` `ssr: false`, inspection copy) | AC Feature "Three.js model view (REQ-010)" (incl. 1 cm spheres, consecutive segments, hover/touch); `npm run test`; `npm run lint`; `npm run build` | Pass | 2026-04-03: `go test ./... -count=1`; `go build ./cmd/server`; `npm run test && npm run lint && npm run build` (`web/`) — green; **`InstancedMesh.count = n`** for all API lights (no LOD); §4.7 / §8.6 (see REQ-010 notes). Re-verified same day after REQ-009 sample geometry update (no UI code change required). |
| TRC-024 | REQ-008, REQ-009, REQ-010 — Requirements block + README (REQ-008) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ008_singleCommandBuildAndRun`; `TestAcceptance_REQ009_defaultSampleModels`; `TestAcceptance_REQ010_threeJsModelView` | Pass | 2026-04-03: `go test ./internal/acceptance -count=1`; asserts `requirementBlock` text for samples spacing/counts/surface, three.js visualization rules, and README mentions **`scripts/run.sh`** + Go workflow. |
| TRC-025 | REQ-009 — Face/surface-even placement vs architecture §3.8 | `backend/internal/samples/samples_test.go` (`assertSphereHemisphereBound`, `assertCubeFaceQuotas` loose/strict interior, `assertConeLateralBaseSplit`) | Same as TRC-022 sample tests (extended in-file assertions) | Pass | 2026-04-03: Executed with `go test ./internal/samples -count=1`; no hemisphere strip dominance (**≤56%** over **48** sample directions); cube **six** nominal faces + **≥85%** loose interior + strict per-face floor / spread; cone **≥80** base-plane + **≥350** lateral hits. |
| TRC-026 | REQ-011 — REST API for per-light state (query and control) | `backend/internal/store/store.go` (`ListLightStates`, `GetLightState`, `PatchLightState`, `ValidateColor`, `ValidateBrightnessPct`, schema + `"on"` column); `backend/internal/httpapi/lightstate.go`, `router.go` (route order per §3.2) | `TestStore_LightStateListPatch` (`store_test.go`); `TestModels_lightStateEndpoints` (`models_test.go`); AC Feature "Per-light state REST API (REQ-011)" (doc scenarios) | Pass | 2026-04-04: `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify ./cmd/server`. API: `GET …/lights/state`, `GET …/lights/{id}/state`, `PATCH …/lights/{id}/state`; `GET /models/{id}` embeds `on`/`color`/`brightness_pct`; defaults **on=true**, **#ffffff**, **100**; invalid light id → **404**; merged PATCH + **200** body matches architecture §3.9. |
| TRC-027 | REQ-012 — Model visualization reflects per-light state | `web/components/ModelLightsCanvas.tsx` (dual **`InstancedMesh`**: **on** = filled + **`instanceColor`** from `lightAppearance`; **off** = **wireframe** + **transparent**; **`Raycaster.intersectObjects`** on both); `web/lib/lightAppearance.ts`; `web/app/models/detail/ModelDetailClient.tsx` (**Apply** → **PATCH** + **`setModel`** merge); `web/lib/models.ts` **`Light`** shape | `lib/lightAppearance.test.ts` (Vitest); `npm run lint`; `npm run build` (`output: "export"`); AC Feature "Visualization reflects light state (REQ-012)" (doc scenarios) | Pass | 2026-04-04: `cd web && npm run lint && npm test && npm run build` — green. Matches **`docs/architecture.md` §4.7**: brightness scales linear RGB; tooltip adds **on**/**color**/**%**; segments unchanged; same-session refresh via React state after **PATCH** (§4.7 state sync). **Note:** optional **≤5 s** multi-client poll not implemented (architecture: optional). |
| TRC-028 | REQ-011 + REQ-012 — Regression / acceptance alignment | Full stack; `docs/acceptance_criteria.md` REQ-011/012 Features | `go test ./... -count=1`; `npm test`; architecture §3.2/§3.9/§4.7/§8.5/§8.7 audit | Pass | 2026-04-04: No boundary violations vs §7 (single Go process + SQLite); API still registered before static/unknown API paths. **Re-verified 2026-04-04:** `TestAcceptance_REQ011_restLightStateAPI` and `TestAcceptance_REQ012_visualizationReflectsLightState` added — see **TRC-029** (prior follow-up closed). |
| TRC-029 | REQ-011 + REQ-012 — Requirements block automation (TRC-017 extension) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ011_restLightStateAPI`; `TestAcceptance_REQ012_visualizationReflectsLightState`; `go test ./internal/acceptance -count=1` | Pass | 2026-04-04: `requirementBlock` asserts REST, per-light state, hex/brightness/PATCH/persist/architecture refs (**REQ-011**); filled/hollow/semi-transparent, REQ-011/010/002 ties, no-reload sync, **0.01 m** sphere (**REQ-012**). Full suite: `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify ./cmd/server`; `cd web && npm run lint && npm test` — all green. |
| TRC-030 | REQ-013 — Batch light state API (architecture §3.10) | `backend/internal/store/store.go` (`BatchPatchLightStates`, `ErrBatchEmptyIDs`, `ErrBatchDuplicateIDs`); `backend/internal/httpapi/lightstate.go` (`patchLightStatesBatch`, **64 KiB** `MaxBytesReader`); `backend/internal/httpapi/router.go` (`PATCH …/lights/state/batch` before per-`lightId` routes) | `TestStore_BatchPatchLightStates` (`store_test.go`); `TestModels_lightStateEndpoints` batch **PATCH** assertion (`models_test.go`); AC Feature "Model view light list pagination and bulk settings (REQ-013)" (API-related scenarios via integration) | Pass | 2026-04-04: `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify ./cmd/server`. **Audit:** single **SQLite** transaction; **200** `{ "states": [...] }` sorted by **`id`**; **400** empty/duplicate/out-of-range **`ids`** and validation failures; **404** unknown model — matches **`docs/architecture.md` §3.2 / §3.10 / §8.8**. **Re-verified 2026-04-04:** `go test ./... -count=1`; `go build -o /tmp/dlm-verify-2 ./cmd/server`. |
| TRC-031 | REQ-013 — Paginated light list, go-to-id, multi-select, bulk apply (architecture §4.8) | `web/app/models/detail/ModelDetailClient.tsx` (`PAGE_SIZE_OPTIONS` **25/50/100**, default **50**; client-side paging over **`GET /models/{id}`**; **Go to light id** + row **`scrollIntoView`**; checkboxes, **select page** + **indeterminate** header; **Shift+click** range on current page; cross-page **`selectedIds`** + **Clear selection**; **`BulkLightStatePanel`** → **`/lights/state/batch`**; **`mergeBatchLightStatesApi`** updates canvas props) | `TestAcceptance_REQ013_modelViewPaginationAndBulkSettings` (`internal/acceptance/requirements_test.go`); `npm run test` (Vitest); `npm run build` (`output: "export"`); AC Feature "Model view light list pagination and bulk settings (REQ-013)" (UI scenarios — doc-to-code audit) | Pass | 2026-04-04: `go test ./internal/acceptance -run TestAcceptance_REQ013 -count=1`; `cd web && npm run test && npm run build` — green. **Audit:** pagination chrome hidden when **`lightCount ≤ 1`** per §4.8 trivial case; bulk + per-row **Apply** coexist; no **hover-only** essential actions; aligns **REQ-002** touch targets (**Button** / inputs). **Re-verified 2026-04-04:** `npm run test`; `npm run build`. |
| TRC-032 | REQ-011 + REQ-012 + REQ-013 — Regression (boundaries unchanged) | Full stack per **`docs/architecture.md` §7** | `go test ./... -count=1`; `go build ./cmd/server`; `npm run test`; `npm run build` | Pass | 2026-04-04: Re-confirmed **one** Go process + API-before-static registration; batch route does not collide with **`GET …/lights/{lightId}/state`**; client **`fetch`** same-origin **`/api/v1/...`**; **§4.7** state sync text covers batch **PATCH** — **`ModelLightsCanvas`** receives updated **`lights`** after bulk merge (**REQ-012** timeliness). **Re-verified 2026-04-04:** same commands — all green. |
| TRC-033 | REQ-013 — Requirements block automation (TRC-017 / TRC-029 extension) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ013_modelViewPaginationAndBulkSettings`; `go test ./internal/acceptance -count=1` | Pass | 2026-04-04: `requirementBlock` asserts **`### REQ-013`** pagination, multi-select, three page-size choices, go-to **id** + feedback, **REQ-005**/**006**/**011**/**012**/**002** ties, bulk **apply**, **persist**, responsive notes, **checkbox**, no **hover-only** essentials. Full regression: `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify-2 ./cmd/server`; `cd web && npm run test && npm run build`. |
| TRC-034 | REQ-012 — Visualization reflects per-light state (on-sphere colour + post-PATCH orbit) | `web/components/ModelLightsCanvas.tsx` (**`MeshBasicMaterial`** for **on** **`InstancedMesh`** with **`instanceColor`** + **`colorFromHexAndBrightness`**; optional **`cameraPersistenceKey`** restores **`OrbitControls.target`** and camera position when **`lights`** updates share the same key); `web/app/models/detail/ModelDetailClient.tsx` (`cameraPersistenceKey={model.id}`) | AC Feature "Visualization reflects light state (REQ-012)" (on-light hex + brightness; visualization updates after successful write); `web/lib/lightAppearance.test.ts`; `npm run test`; `npm run lint`; `npm run build`; `go test ./... -count=1` | Pass | 2026-04-04: **Audit vs `docs/architecture.md` §4.7:** **`vertexColors: true`** on **on** material was incorrect with **no** geometry **`color`** attribute (Three.js multiplies **vColor** by missing/zero vertex colour → black spheres). Default **`vertexColors`** preserves **`instanceColor`** path. **`cameraPersistenceKey`** avoids reframing on each **`lights`** identity change after **PATCH** while keeping **§4.7** state sync (client merge → props → scene rebuild). **Regression:** `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify-verifier ./cmd/server`; `cd web && npm run test && npm run lint && npm run build` — all green. **Manual:** confirm sphere tint and orbit after **Apply** in browser. |
| TRC-035 | REQ-014 — Default light state (off, 100% white) and model reset | `backend/internal/store/store.go` (`DefaultLightOn=false`, `ResetAllLightStates`, insert defaults); `backend/internal/httpapi/lightstate.go` (`postResetLightStates`); `backend/internal/httpapi/router.go` (`POST /models/{id}/lights/state/reset`); `web/app/models/detail/ModelDetailClient.tsx` (**Reset lights** + `mergeBatchLightStatesApi`) | AC Feature "Default light state and model reset (REQ-014)"; `TestStore_ResetAllLightStates`; `TestModels_lightStateEndpoints` (POST reset after batch); `go test ./... -count=1` | Pass | 2026-04-04: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify ./cmd/server`**. Reset returns **200** `{ "states": [...] }` for all lights **off** / **`#ffffff`** / **100%**; **404** unknown model. UI button non–hover-only (full width on narrow viewports). |
| TRC-036 | REQ-010 — Three.js view (2 cm spheres; #D0D0D0 segments at 85% transparency) | `web/lib/wireSegments.ts` (`SPHERE_RADIUS_M = 0.01`); `web/components/ModelLightsCanvas.tsx` (`LineBasicMaterial` + **`MeshBasicMaterial`** off layer **`0xd0d0d0`**, **opacity 0.15**); `web/app/models/detail/ModelDetailClient.tsx` | AC Feature "Three.js model view (REQ-010)" (updated diameter + segment colour/transparency); `web/lib/wireSegments.test.ts`; `npm run test`; `npm run lint`; `npm run build`; `TestAcceptance_REQ010_threeJsModelView` | Pass | 2026-04-04: **`cd web && npm run test && npm run lint && npm run build`**; **`cd backend && go test ./internal/acceptance -run TestAcceptance_REQ010 -count=1`**. Consecutive **id** segments only (**`buildWireSegmentPositions`**). **Supersedes** TRC-023 narrative on **1 cm** / white segment lines for current product behaviour. **Updated 2026-04-04:** softer **`#D0D0D0`** + **15%** opacity per **`docs/requirements.md`**. |
| TRC-037 | REQ-012 — Off lights match segment styling (#D0D0D0, 85% transparency) | `web/components/ModelLightsCanvas.tsx` (**off** = filled **`MeshBasicMaterial`**, not wireframe; same grey/opacity as wire lines); `web/lib/lightAppearance.ts` (unchanged **on** colour × brightness) | AC Feature "Visualization reflects light state (REQ-012)"; `TestAcceptance_REQ012_visualizationReflectsLightState`; `npm run test`; `npm run build` | Pass | 2026-04-04: Doc automation + **`npm run test`/`build`** green. **§4.7** state sync extended in code comments/effect deps to include **`POST …/reset`** (client merge). **Supersedes** TRC-027/034 **wireframe-off** narrative where it conflicts with current REQ-012 text. **Updated 2026-04-04:** **`#D0D0D0`** / **opacity 0.15** aligned with REQ-010. |
| TRC-038 | REQ-011 — Per-light REST + defaults tied to REQ-014 | `backend/internal/store/store.go` (`DefaultLightOn=false`, schema **`"on"` DEFAULT 0** on new/`ALTER`); `backend/internal/httpapi/lightstate.go` | `TestStore_CreateListGetDelete`, `TestStore_LightStateListPatch`, `TestStore_BatchPatchLightStates`, `TestModels_lightStateEndpoints` (initial **GET …/lights/state** both **off**); `TestAcceptance_REQ011_restLightStateAPI` | Pass | 2026-04-04: **`go test ./... -count=1`**. **TRC-026** row text citing **on=true** default is **obsolete**; this row is authoritative for **off** default + **#ffffff** + **100%** brightness. |
| TRC-039 | REQ-005 — Chain adjacency (at most two neighbours by consecutive id) | `docs/requirements.md` REQ-005 rule 6; `web/lib/wireSegments.ts` **`buildWireSegmentPositions`** (sorted **id**, edges **(i,i+1)** only); `ModelLightsCanvas` segment geometry | AC Feature "Wire light model definition" (REQ-005 chain scenario); implementation review vs **`docs/architecture.md` §3.6** chain note | Pass | 2026-04-04: No extra edges in segment builder; **n ≤ 1** → no segments. |
| TRC-040 | REQ-015 — Scenes: composite space, placement, CRUD, and three.js view | `backend/internal/store/store.go` (`ensureSceneTables`, `ensureSceneModelOrdinal`, `Delete` pre-check); `backend/internal/store/scenes.go` (`CreateScene` with ordered `modelIDs`, `OffsetsForFirstSceneModel`, `DefaultOffsetsForNewModel`, `scene_models.ordinal`); `backend/internal/httpapi/scenes.go` (`POST /scenes` JSON **`model_id` only**, `DisallowUnknownFields`); `json.go` (`writeAPIErrorDetail`); `backend/internal/httpapi/router.go`; `web/lib/scenes.ts`; `web/components/SceneLightsCanvas.tsx`; `web/app/scenes/*` | `TestScenes_CreateGetDelete`, `TestScenes_CreateSceneAutoOffsetsNegativeModelCoords`, `TestScenes_CreateScenePreservesModelOrder`, `TestScenes_LastModelRemoval` (`store/scenes_test.go`); `TestScenes_createAndGet`, `TestScenes_createRejectsClientOffsets` (`httpapi/scenes_test.go`); AC Feature "Scenes composite 3D space and management (REQ-015)" | Pass | 2026-04-04 (re-verify): `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify-verifier ./cmd/server`; `cd web && npm run lint && npm run test && npm run build`. **§3.12–§3.13:** server-computed integer offsets on create; **400** if client sends offset fields; canonical **`lights`** unchanged; **`GET` scene** exposes **`x,y,z`** + **`sx,sy,sz`**; **`scene_models.ordinal`** preserves **JSON** array order; **`+X`** placement for models after the first; **`scene_last_model`** **409**; **`GetScene`** closes **`rows`** before nested loads. **§4.9:** `/scenes/new` — name + ordered models (Up/Down), no create-time offset inputs. |
| TRC-041 | REQ-006 — Model delete blocked when model is used in scenes (rule 5) | `backend/internal/store/store.go` (`Delete` → **`ModelInUseError`** + **`listScenesReferencingModel`**); `backend/internal/httpapi/models.go` (`deleteModel` → **409** **`model_in_scenes`** + **`details.scenes`**); `web/app/models/page.tsx` (409 message + scene names) | `TestModels_delete409WhenInScene` (`httpapi/scenes_test.go`); AC Feature "Model management" scenario "REQ-006 blocks model delete when model is used in scenes and explains why" | Pass | 2026-04-04 (re-verify): Same regression commands as **TRC-040** row above; **409** body includes **`error.code`** **`model_in_scenes`** and **`details.scenes`** with **`id`**/**`name`**. **TRC-012** remains valid for core CRUD; this row records the **REQ-015** interaction. |
| TRC-042 | REQ-015 — Architecture alignment: auto placement + create payload (post–§3.12 revision) | `docs/architecture.md` §3.12–§3.13, §4.9, §8.10; implementation in **TRC-040** components | Doc-to-code audit (no new automated test); regression: `go test ./... -count=1`; `npm run build` | Pass | 2026-04-04: **`@verifier`** audit — **`POST /api/v1/scenes`** matches architecture (**ordered** **`model_id`** list; server placement; reject extra keys per element); **`CreateScene`** matches create-time algorithm; **`/scenes/new`** omits offset fields. **Optional gap:** still no **`TestAcceptance_REQ015_*`** in **`internal/acceptance`** (**TRC-040** integration tests + AC review remain the gate). |
| TRC-043 | REQ-016 — Camera reset for model and scene 3D views | `web/components/ModelLightsCanvas.tsx`, `web/components/SceneLightsCanvas.tsx` (`cameraResetVersion` forces default orbit vs `cameraPersistenceKey` restore); `web/app/models/detail/ModelDetailClient.tsx`, `web/app/scenes/detail/SceneDetailClient.tsx` (**Reset camera** `Button`) | **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`** (`internal/acceptance/requirements_test.go`); AC Feature "Camera reset for 3D views (REQ-016)"; `npm run lint`; `npm run test`; `npm run build` (`/models/detail`, `/scenes/detail` export) | Pass | 2026-04-04: **`cd web && npm run lint && npm test && npm run build`** — green. **Audit vs `docs/architecture.md` §4.7 / §4.9 / §8.14:** reset is **client-only** (no `fetch`); default framing matches initial **`boundingFromLights`** + **`OrbitControls.target`** + camera offset recipe; **touch**/**full-width** buttons on narrow viewports (**REQ-002**). **Updated 2026-04-06:** **`requirementBlock("REQ-016")`** assertions in **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`**; **`go test ./internal/acceptance -run TestAcceptance_REQ016 -count=1`** — Pass. **Manual:** orbit after pan/zoom then **Reset camera** in browser. |
| TRC-044 | REQ-017 — Options: factory reset with confirmation | `backend/internal/store/store.go` (`seedThreeCanonicalSamplesTx`, `FactoryReset`, `SeedDefaultSamples` refactor); `backend/internal/httpapi/system.go`; `backend/internal/httpapi/router.go` (**`POST /system/factory-reset`** registered first on API mux); `web/app/options/page.tsx` (`<dialog>` + **Cancel** / **Erase everything**); `web/app/models/page.tsx` (**`Suspense`** + **`factoryReset=1`** success banner + `router.replace`) | **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`** (`internal/acceptance/requirements_test.go`); `TestStore_FactoryReset` (`store_test.go`); `TestAPIv1FactoryReset_resetsToThreeSamples` (`handlers_test.go`); AC Feature "Options factory reset with confirmation (REQ-017)" | Pass | 2026-04-04: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-pass ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`**. **Audit vs §3.14 / §4.10 / §8.13:** single **SQLite** transaction **DELETE** scenes → lights → models → seed; **200** `{ "ok": true }`; modal copy warns **permanent** loss and **three samples** only; **unauthenticated** endpoint noted in architecture §9 (operator LAN exposure). **Route ordering:** **`/system/factory-reset`** not shadowed by **`GET /{path...}`**. **Updated 2026-04-06:** **`requirementBlock("REQ-017")`** assertions in **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`**; **`go test ./internal/acceptance -run TestAcceptance_REQ017 -count=1`** — Pass. |
| TRC-045 | REQ-018 — Application shell: themes, collapsible navigation, branding, and Font Awesome controls | `web/app/layout.tsx` (inline **`dlm-theme`** script + **`AppShell`**); `web/components/AppShell.tsx` (**burger**, **`faLightbulb`** regular + title **`Domestic Light & Magic`**, **`faSun`/`faMoon`** toggle, left **`aside`** nav **`/`** / **`/models`** / **`/scenes`** / **`/options`**, **`localStorage`** **`dlm-theme`**, mobile drawer + desktop collapse); `web/components/UiThemeContext.tsx`; `web/tailwind.config.ts` (**`darkMode: 'class'`**); `web/lib/theme.ts`; `web/lib/theme.test.ts`; `web/components/ui/Button.tsx`, `web/components/ui/ButtonLink.tsx`; `web/components/ModelLightsCanvas.tsx`, `web/components/SceneLightsCanvas.tsx` (**`isDarkDom`** / **`uiTheme`** deps); page clients under **`web/app/**`** ( **`Button`** **`icon=`** usage) | AC Feature "Application shell themes navigation branding and Font Awesome (REQ-018)" (doc-to-code audit); **`npm run test`** (**`lib/theme.test.ts`**); **`npm run lint`**; **`npm run build`** (`output: "export"`); **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req018 ./cmd/server`** (regression) | Pass | 2026-04-04: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req018 ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`** — all **Pass**. **Audit vs `docs/architecture.md` §4.11:** class-based **Tailwind** **`dark:`** on **`html`**; default **light** + persist **`dlm-theme`**; shell **white** / **gray-900** surfaces; **Font Awesome** **SVG** packages + **`@fortawesome/react-fontawesome`**; **REQ-010**/**012** canvases follow **`html.dark`**. **Optional gap:** no **`TestAcceptance_REQ018_*`** in **`internal/acceptance`** yet (**TRC-017** parity); gate remains **TRC-045** audit + **`theme.test.ts`** + **`npm run build`**. **Boundaries:** verifier made **no** application source edits. **Superseded for theme default:** see **TRC-047** ( **`prefers-color-scheme`** until **`dlm-theme`** set). **Superseded for 3D backdrop:** see **TRC-046** (**REQ-019** fixed grey; canvases no longer track shell via **`isDarkDom`**). |
| TRC-046 | REQ-019 — Three.js model and scene views: fixed dark-grey viewport | `web/lib/vizViewport.ts` (**`VIZ_VIEWPORT_BG`**, **`VIZ_VIEWPORT_BG_CSS`**); `web/components/ModelLightsCanvas.tsx`, `web/components/SceneLightsCanvas.tsx` (**`scene.background`**, **`renderer.setClearColor`**, grid helper tuned for dark viz, wrapper **`backgroundColor`** **`#262626`**, empty-state scrim); `docs/architecture.md` §4.7 / §4.9 | AC Feature "Three.js fixed dark-grey viewport (REQ-019)" (doc-to-code audit); **`web/lib/vizViewport.test.ts`**; **`npm run test`**; **`npm run lint`**; **`npm run build`**; **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-20260404 ./cmd/server`** | Pass | 2026-04-04: Full regression green. **Audit vs architecture:** WebGL clear and scene fill use **fixed** **`0x262626`** independent of **`html.dark`**; letterbox wrapper matches **CSS** hex; grid opacity/colours aligned to dark viz (**§4.7**). **Optional gap:** no **`TestAcceptance_REQ019_*`** in **`internal/acceptance`** — AC + **TRC-046** audit + **`vizViewport.test.ts`** gate. Verifier made **no** application source edits. |
| TRC-047 | REQ-018 — Default theme follows system preference until user overrides | `web/lib/theme.ts` (**`resolveInitialTheme`**); `web/app/layout.tsx` (inline script: valid **`dlm-theme`** wins; else **`matchMedia('(prefers-color-scheme: dark)')`**); `web/components/AppShell.tsx` (**`useLayoutEffect`** sync); `web/lib/theme.test.ts` (stored vs system vs **`matchMedia`** throw) | AC Feature REQ-018 scenario "defaults theme to system preference until user overrides"; **`TestAcceptance_REQ018_applicationShellThemesNavAndFontAwesome`** (requirements block); **`npm run test`** (**`theme.test.ts`**); **`npm run lint`**; **`npm run build`**; **`go test ./... -count=1`** | Pass | 2026-04-04: Same regression commands as **TRC-046** row — all **Pass**. **Audit vs `docs/architecture.md` §4.11 / §8.1:** first paint script + **`resolveInitialTheme`** match **REQ-018** business rule 1. Verifier made **no** application source edits. |
| TRC-048 | REQ-020 — Scene spatial API for dimensions, filtered retrieval, and bulk updates | `backend/internal/httpapi/router.go`, `backend/internal/httpapi/scenes.go`, `backend/internal/store/scenes.go` | `TestScenes_SpatialQueriesAndDimensions`, `TestScenes_SpatialBulkUpdateAndInvalidGeometry` (`internal/store/scenes_test.go`); `TestScenes_spatialEndpointsDimensionsAndQueries`, `TestScenes_spatialBulkUpdateAndInvalidGeometry` (`internal/httpapi/scenes_test.go`); `cd backend && go test ./internal/store -run "TestScenes_Spatial" -count=1`; `cd backend && go test ./internal/httpapi -run "TestScenes_spatial" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Verifier audit vs `docs/architecture.md` §3.15 / §8.15 found no boundary violations. New routes are registered on `/api/v1/scenes/{id}/...` for dimensions, full lights, cuboid/sphere query, and cuboid/sphere bulk state updates. Store enforces finite + positive geometry validation (`ErrSceneInvalidGeometry`), inclusive cuboid/sphere containment, scene-space matching (`sx/sy/sz`), and transactional bulk updates with rollback semantics (no partial writes). Tests confirm dimensions payload shape, scene-coordinate retrieval/filtering, bulk update behavior, invalid-geometry rejection (`validation_failed`), and canonical model coordinates remaining unchanged. |
| TRC-049 | REQ-014 — Default light state (off, 100% white) and model reset (requirements block automation) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ014_defaultLightStateAndReset`; `cd backend && go test ./internal/acceptance -run "TestAcceptance_REQ014" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Added dedicated `requirementBlock("REQ-014")` assertions for default off/white/100 state, reset control semantics, responsive/non-hover requirements, and REQ dependency links. |
| TRC-050 | REQ-015 — Scenes: composite space, placement, CRUD, and three.js view (requirements block automation) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ015_scenesCompositeSpacePlacementCRUD`; `cd backend && go test ./internal/acceptance -run "TestAcceptance_REQ015" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Added dedicated REQ-015 requirements assertions covering create/list/delete/open scene lifecycle, automatic integer placement on create, canonical-vs-derived coordinate separation, non-negative containment, +X add-model default, last-model confirmation flow, and required dependencies. |
| TRC-051 | REQ-019 — Three.js model and scene views: fixed dark-grey viewport (requirements block automation) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ019_fixedDarkGreyThreeJSViewport`; `cd backend && go test ./internal/acceptance -run "TestAcceptance_REQ019" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Added dedicated REQ-019 requirements assertions for fixed dark-grey viewport policy across shell themes, REQ-018/010/015/002 linkage, and responsive/non-hover constraints. |
| TRC-052 | REQ-020 — Scene spatial API for dimensions, filtered retrieval, and bulk updates (requirements block automation) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ020_sceneSpatialAPI`; `cd backend && go test ./internal/acceptance -run "TestAcceptance_REQ020" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Added dedicated REQ-020 requirements assertions for dimensions endpoint, all-lights retrieval in scene coordinates, cuboid/sphere query + bulk update operations, REQ-011 state semantics, canonical coordinate immutability, invalid-geometry rejection, and dependency references. |
| TRC-053 | REQ-020 — Scene whole-scene uniform patch and per-light batch patch (`PATCH …/lights/state/scene`, `PATCH …/lights/state/batch`) | `backend/internal/store/scenes.go` (`PatchSceneLightsScene`, `PatchSceneLightsBatch`); `backend/internal/httpapi/scenes.go`, `router.go` | `TestPatchSceneLightsSceneAndBatch`, `TestPatchSceneLightsBatchRejectsForeignLight` (`internal/store/scenes_test.go`); `go test ./... -count=1` | Pass | 2026-04-05: Audit vs `docs/architecture.md` §3.15 — routes and transactional store paths; batch membership validation; 4 MiB body cap; canonical `lights` coords unchanged. |
| TRC-054 | REQ-021 — Scenes: routines (definitions, run/stop, first random-colour type) | `backend/internal/store/routines.go`; `backend/internal/store/store.go`; `backend/internal/httpapi/routines.go`, `routine_scheduler.go`, `router.go`; `backend/cmd/server/main.go`; `web/lib/routines.ts`, `web/app/routines/page.tsx`, `web/app/scenes/detail/SceneDetailClient.tsx`, `web/components/AppShell.tsx` | `TestRoutinesCreateStartStopDelete` (`internal/store/scenes_test.go`); AC Feature "Scene routines definitions run stop and first random colour type (REQ-021)"; `go test ./... -count=1`; `npm run lint`; `npm test`; `npm run build` | Pass | 2026-04-05: Audit vs §3.14–§3.16, §6.2, §7 DB label; optional gap: no `TestAcceptance_REQ021_*` yet. |
| TRC-055 | REQ-017 — Factory reset removes routines and routine runs | `backend/internal/store/store.go` (`FactoryReset` delete order) | `TestStore_FactoryReset`; `TestAPIv1FactoryReset_resetsToThreeSamples`; `go test ./... -count=1` | Pass | 2026-04-05: Confirms `routine_runs` then `routines` deleted before scenes in same transaction; supplements **TRC-044** where narrative still says scenes-first only. |
| TRC-056 | REQ-022 — Python scene routines: editor, Pyodide worker, scene API, persistence, and forced stop | `backend/internal/store/routines.go`, `store.go`; `backend/internal/httpapi/routines.go`, `routine_scheduler.go`, `router.go`; `web/public/dlm-python-scene-worker.mjs`, **`web/public/dlm-python-editor-worker.mjs`**; **`web/lib/pythonEditorWorker.ts`**, **`web/lib/pythonRoutineCodemirror.ts`**; `web/components/PythonRoutineHost.tsx`; `web/app/routines/python/PythonRoutineEditorClient.tsx`, `page.tsx`; `web/app/scenes/detail/SceneDetailClient.tsx`; `web/lib/routines.ts`; `web/app/routines/page.tsx`; **`docs/architecture.md`** §3.17 / §4.13 (**CodeMirror** **6** **only**) | AC Feature "Python scene routines editor API docs and execution (REQ-022)" (doc review); `internal/store/routines_python_test.go`; **`lib/pythonRoutineCodemirror.test.ts`**; **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`**; `go test ./internal/acceptance -run TestAcceptance_REQ022 -count=1`; `cd backend && go test ./... -count=1`; `cd web && npm run lint && npm test && npm run build` | Pass | 2026-04-05: **`go test ./... -count=1`**; **`npm run lint`**, **`npm test`**, **`npm run build`** — all **Pass**. **Audit vs §3.17 / §4.13 / REQ-022:** **`PythonRoutineHost`** **`T_FORCE_MS` = **5000**; **editor** **CodeMirror** **6** **only** (**no** **Monaco** **in** **`web/package.json`**; **`@uiw/react-codemirror`** + **`@codemirror/*`** **^6**); **`lintGutter`** + **Pyodide** **`ast.parse`** (**`dlm-python-editor-worker.mjs`**); **`scene.`** completions; **format-on-save** (**`black`** / **`textwrap`**). **`TestAcceptance_REQ022_*`** locks **REQ-022** **text**, **deps**, **and** **`PythonRoutineEditorClient.tsx`**. **Optional gap:** no dedicated **`internal/httpapi`** routine **GET/PATCH** tests (store tests + regression remain the gate). |
| TRC-057 | REQ-023 — Create routine: single type dropdown (includes Python) | `web/app/routines/new/RoutineNewClient.tsx`, `web/app/routines/new/page.tsx`, `web/app/routines/page.tsx`, `web/app/routines/python/PythonRoutineEditorClient.tsx`, `web/app/routines/python/new/page.tsx`, `web/lib/routines.ts` (`ROUTINE_TYPE_CREATE_OPTIONS`, `isCreatableRoutineType`) | AC Feature "Create routine type selection dropdown (REQ-023)" (Gherkin scenarios reference **`docs/requirements.md`**); **`web/lib/routines.test.ts`** (**`routine types (REQ-023)`**); `cd backend && go test ./... -count=1`; `cd web && npm run lint && npm test && npm run build` | Pass | 2026-04-05: **Audit vs `docs/architecture.md` §4.11–§4.13:** **`/routines/new`** hosts labeled **`type`** **`<select>`** with **`random_colour_cycle_all`** and **`python_scene_script`**; **`/routines`** has a single **New routine** action (no standalone primary **Python-only** create button); **`/routines/python/new`** **`redirect`** → **`/routines/new?type=python_scene_script`**; **`PythonRoutineEditorClient`** without **`id`** **`replace`**s to the same; **`AppShell`** **`NAV`** lists **`/routines`** only (no Python-only create nav item). **`POST /api/v1/routines`** unchanged (client sends **`type`** + **`python_source`** when Python). **Optional gap:** no **`TestAcceptance_REQ023_*`** in **`internal/acceptance`** — gate is **TRC-057** + **`routines.test.ts`** + doc AC review. **Supplements** **TRC-054** (REQ-021 routine list/run UX; create path now REQ-023). Verifier made **traceability-only** edits. |

**Verifier notes (architecture):** Repository matches §2 layout intent (`backend/cmd/server`, `internal/config`, `internal/httpapi`, `internal/webdist`; `web/` source-only). **§7 flowchart:** one Go process serves `/health`, `/api/v1/*`, and embedded static export. **§8:** initial load and `fetch /api/v1/status` paths satisfied. **§3.2 ordering:** API and `/health` registered before `/` static handler. **Boundaries:** `backend` does not import `web` TS; built assets land in `internal/webdist/dist` via `scripts/copy-web-export.mjs` aligning with §3.5. **SPA fallback (§3.2):** implemented in `staticExportHandler` without violating API precedence.

**Verifier notes (REQ-005–REQ-007, 2026-04-03):** **§2 / §3.1** — `internal/wiremodel` and `internal/store` present as specified. **§3.2** — Model routes match table (`GET/POST /models`, `GET/DELETE /models/{id}` on the `/api/v1` submux). **§3.3** — SQLite + transactional create; FK pragma via DSN. **§3.6** — CSV rules implemented in `wiremodel.ParseLightsCSV`. **§4.6** — Models UI uses list/new/detail with client `fetch`; detail URL is `/models/detail?id=<uuid>` for static export compatibility (acceptable deviation from literal `/models/[id]` path). **§9** — `MaxBytesReader` caps upload size on create. No architectural violations requiring implementor rework.

**Verifier notes (REQ-008–REQ-009, 2026-04-03, post–§3.8 face/surface-even samples):** **§3.7** — `scripts/run.sh` unchanged intent. **§3.8 / code** — `segmentCountForChordBand` + `subdivideEdge` / `subdivideGreatCircleArc` enforce **0.05–0.10 m** consecutive chords; **sphere**: **Fibonacci** seeds + greedy **nearest-neighbor** order + great-circle subdivision (**no** stacked latitude rings); **cone**: **`coneBaseSpiral`** on **z=0** + **`coneRingAtZ`** lateral rings + **ruling** to **apex** + **`trimConeChain`** to target **n**; **cube**: **six** **face serpentines** + **`appendCubeBridgePoints`** (axis-aligned **surface** legs, `pointOnCubeSurface` / `axisSegmentOnCubeSurface`). Tests: **TRC-022** / **TRC-025**; **README** default samples bullet still **5–10 cm** and **500–1000** lights. **TRC-024** locks requirements.md (+ README for REQ-008) wording.

**Verifier notes (REQ-010, 2026-04-03, re-verified after §4.7 expansion):** Matches **`docs/architecture.md` §4.6–§4.7, §8.4–§8.6**: detail route **`/models/detail?id=`** + **`dynamic(..., { ssr: false })`**; **`InstancedMesh`** + **`SphereGeometry(SPHERE_RADIUS_M=0.005)`** + white **`MeshStandardMaterial`**; ambient + directional light; **`LineSegments`** from **`buildWireSegmentPositions`** (ascending **`id`**, **`wireSegments.test.ts`**); **`LineBasicMaterial`** **`transparent`** / **`opacity`** ≈ **0.28**; framing uses **`boundingFromLights`** + **sphere margin**; **`OrbitControls`**; **`Raycaster`** → **`instanceId`** with **fixed** DOM tooltip (**id** + **x,y,z** to **4** decimals); **pointer** hover (**mouse** only, rAF-throttled); **tap** pick vs orbit via **≤10 px** movement; miss clears pin; **`ResizeObserver`** on canvas host; empty **`lights`** still initializes scene + overlay. Requirements text locked by **`TestAcceptance_REQ010_threeJsModelView`** (**TRC-024**).

**Verifier notes (REQ-011 / REQ-012, 2026-04-04):** **§3.2** — `GET /models/{id}/lights/state`, `GET /models/{id}/lights/{lightId}/state`, and `PATCH …/state` are registered **before** `GET /models/{id}` so `lights/state` is not parsed as a `lightId`. **§3.3 / §3.9** — `lights` carries **`"on"`** (quoted), **`color`**, **`brightness_pct`**; migration adds columns on legacy DBs; **`MaxBytesReader`** on **PATCH** (**8 KiB**). **§4.7** — **on** spheres use **`MeshBasicMaterial`** + **`instanceColor`** (acceptable per architecture’s MeshBasic option); **off** uses **wireframe** + **opacity** ~**0.36**; **OrbitControls** + **segments** preserved. **§8.7** — persistence in **Go**, client merges **200** JSON into **`ModelDetail`** so the canvas **`useEffect([lights])`** rebuilds without full page reload. **TRC-023** remains valid for **1 cm** geometry, **consecutive** segments, and picking; appearance is now state-driven per **REQ-012**.

**Verifier notes (REQ-011 / REQ-012 acceptance tests, 2026-04-04):** **TRC-029** records **`TestAcceptance_REQ011_restLightStateAPI`** and **`TestAcceptance_REQ012_visualizationReflectsLightState`**, extending the **TRC-017** pattern to **`### REQ-011`** / **`### REQ-012`** in **`docs/requirements.md`**. **Updated 2026-04-06:** **TRC-009** and **TRC-017** titles now span **REQ-001–REQ-017** (rollup); per-REQ rows **TRC-018**–**TRC-033** and **TRC-043**–**TRC-044** remain authoritative. **REQ-013** automation is **TRC-031** / **TRC-033**. **REQ-016** / **REQ-017** use **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`** and **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`** (**TRC-043** / **TRC-044**). **REQ-015** still has no **`TestAcceptance_REQ015_*`** — **TRC-040** / **TRC-042** optional gap unchanged.

**Verifier notes (REQ-013, 2026-04-04):** **§3.2 / §3.10** — **`PATCH /api/v1/models/{id}/lights/state/batch`** is registered on the **`/api/v1`** submux immediately after **`GET …/lights/state`** and before **`…/lights/{lightId}/state`**, avoiding pattern shadowing. **Store** applies duplicate detection, range check via model **light count**, and **per-row** merge semantics consistent with **`PatchLightState`**. **§4.8** — UI uses **in-memory** paging over the single detail payload (**no** extra list endpoint), **three** page sizes **25 / 50 / 100**, and **bulk** **PATCH** for **REQ-011** fields. **Doc drift:** **`TestAcceptance_REQ013_modelViewPaginationAndBulkSettings`** is recorded under **TRC-031** (implementation + UI) and **TRC-033** (requirements-block automation, **TRC-029** pattern).

**Verifier notes (full re-pass, 2026-04-04):** **`@verifier`** re-run: architectural spot-check — no new violations vs **`docs/architecture.md` §3.2 / §3.10 / §4.7–§4.8 / §7–§8**; **`go test ./... -count=1`** and **`go build -o /tmp/dlm-verify-2 ./cmd/server`** (`backend/`); **`npm run test`** and **`npm run build`** (`web/`) — all **Pass**. **TRC-030**–**032** verification text amended with **Re-verified** lines; **TRC-033** added for REQ-013 **`requirementBlock`** automation.

**Verifier notes (REQ-012 on-material + orbit, 2026-04-04, TRC-034):** **`TestAcceptance_REQ012_visualizationReflectsLightState`** and **`lib/lightAppearance.test.ts`** still cover requirements text and linear brightness; WebGL appearance is **manual** per §4.7 testing note. Prior matrix text mentioning **`MeshStandardMaterial`** for **on** spheres (e.g. TRC-023 narrative) reflected an earlier sketch; current **`MeshBasicMaterial` + `instanceColor`** remains within §4.7’s allowed patterns.

**Verifier notes (REQ-010 / REQ-012 / REQ-014 pass, 2026-04-04):** Full **`@verifier`** re-run after implementor changes: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify ./cmd/server`**; **`cd web && npm run test && npm run lint && npm run build`** — all **Pass**. **§3.2:** **`POST …/lights/state/reset`** registered with **`GET …/lights/state`** and **`PATCH …/state/batch`** before **`…/lights/{lightId}/state`** and **`GET /models/{id}`**. **§3.9 / §3.11:** **`ResetAllLightStates`** + HTTP handler match architecture. **§4.7:** **2 cm** spheres (**`radius 0.01`**), wire + **off** spheres use **`#D0D0D0`** at **opacity 0.15** (**85%** transparent) per current **`docs/requirements.md`**. **TRC-035**–**TRC-039** record trace rows; older rows (**TRC-023**, **TRC-026**, **TRC-027**, **TRC-029**) may still mention **1 cm**, **on=true** default, or **wireframe off** — treat **TRC-036**–**TRC-038** as superseding those aspects.

**Verifier notes (REQ-015 + REQ-006 scene references, 2026-04-04):** Architectural audit vs **`docs/architecture.md` §3.12–§3.13, §4.9, §7–§8.10–§8.12**: scene REST surface and SQLite schema match the documented table; **`model_in_scenes`** and **`scene_last_model`** error envelopes use **`writeAPIErrorDetail`**; API routes remain on the **`/api/v1`** submux ahead of static handling. **§8.4** delete-model sequence extended with **409** branch in code and tests. **Optional gap (non-blocking):** no **`TestAcceptance_REQ015_*`** in **`internal/acceptance/requirements_test.go`** yet—requirements text for **REQ-015** is validated by **`docs/acceptance_criteria.md`** scenario review plus **TRC-040** integration tests; add **`requirementBlock`** automation via **`@implementor`** if full **TRC-017** parity is desired.

**Verifier notes (REQ-015 re-verify, 2026-04-04, server-computed create):** Full pass: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-verifier ./cmd/server`**; **`cd web && npm run lint && npm run test && npm run build`**. **TRC-040** row updated (replaces obsolete **`TestScenes_RejectNegativeContainment`** narrative — negative model coords are shifted by **`OffsetsForFirstSceneModel`** on create). **TRC-042** records explicit architecture doc alignment for auto placement + strict create JSON. **Boundaries:** verifier made **no** application source edits.

**Verifier notes (REQ-016 / REQ-017, 2026-04-04; acceptance tests 2026-04-06):** Full pass: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-pass ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`**. **§3.2:** **`POST /api/v1/system/factory-reset`** is on the **`/api/v1`** submux before catch-all **`GET /{path...}`**. **§3.14:** **`FactoryReset`** matches documented delete order + **`seedThreeCanonicalSamplesTx`**. **§4.10:** **`/options`** static route exported; confirmation before POST; redirect to **`/models`** with query then strip (**architecture default**). **§4.7 / §4.9:** camera reset via **`cameraResetVersion`** preserves **REQ-010**/**012** rendering while resetting orbit. **`internal/acceptance`:** **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`** and **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`** lock **`### REQ-016`** / **`### REQ-017`** text in **`docs/requirements.md`** (**TRC-043**, **TRC-044**; **TRC-009** / **TRC-017** rollups updated). **Boundaries:** traceability-only edits in this pass.

**Verifier notes (REQ-018, 2026-04-04):** **`@verifier`** pass recorded as **TRC-045**. Full regression: **`go test ./... -count=1`**; **`go build ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`**. **§4.11** satisfied by **`AppShell`** + **`layout`** theme script + **`Button`**/**`ButtonLink`** icon rule + left nav entries. **`TRC-009`**/**`TRC-017`** rollups in this file still list **REQ-001–REQ-017** in prose; **REQ-018** is covered by **TRC-045** until those summary rows are editorially refreshed.

**Verifier notes (REQ-018 default + REQ-019 viz, 2026-04-04):** **`@verifier`** re-run after implementor: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-20260404 ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`** — all **Pass**. **TRC-046** records **REQ-019** (**§4.7** / **§4.9** fixed **`#262626`** viewport). **TRC-047** records **REQ-018** rule 1 (**`prefers-color-scheme`** when **`dlm-theme`** unset). **TRC-045** narrative on “default light” and “canvases follow **`html.dark`**” is **obsolete** for those points; use **TRC-046** / **TRC-047**. **Optional:** add **`TestAcceptance_REQ019_*`** for **TRC-017** parity.

**Verifier notes (REQ-020 extension + REQ-021 + REQ-017 routine wipe, 2026-04-05):** Full pass: **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test && npm run build`**. **§3.2:** literal segments `routines`, `state/scene`, `state/batch`, `routines/runs`, `start`, `stop` registered before ambiguous `{id}` patterns where applicable. **§3.15–§3.16:** scene batch + whole-scene patch and routine lifecycle align with architecture; **§7** diagram note updated in code (`routines` + `routine_runs` in SQLite). **TRC-053**–**TRC-055** record trace rows; **TRC-048**/**TRC-052** remain valid for cuboid/sphere spatial API baseline.

**Verifier notes (REQ-022, 2026-04-05 re-verify):** Full regression **`go test ./... -count=1`**; **`npm run lint`**, **`npm test`**, **`npm run build`** — all **Pass**. **TRC-056** updated to **Pass** — **`T_force`** = **5000 ms**; **Pyodide** **`ast.parse`** diagnostics + **`scene.`** completions + **format-on-save** (**`black`** / fallback) match **§3.17** / **REQ-022** rules **3–4** at documented intent. **Manual / environment:** first **Pyodide** load and **micropip** **`black`** install require **network** and can take **tens of seconds** on slow links (architecture §6.2 trade-off). Verifier made **no** application source edits (traceability-only).

**Verifier notes (REQ-022 CodeMirror 6 mandate, 2026-04-05):** **`docs/requirements.md`** **REQ-022** **rule** **2** and **`docs/architecture.md`** **§3.17** / **§4.13** require **CodeMirror** **6** **only** for the **Python** **routine** **buffer**; implementation and **`web/package.json`** match (**no** **Monaco**). **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`** + **`TRC-056`** / **`TRC-009`** / **`TRC-017`** updates record automation. Verifier made **traceability-only** edits.

**Verifier notes (REQ-023, 2026-04-05):** **`@verifier`** pass recorded as **TRC-057**. Full regression: **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test && npm run build`** — all **Pass**. No Mermaid boundary violations: create flow remains browser **`fetch`** → same-origin **`POST /api/v1/routines`** per §5. **TRC-009** / **TRC-017** rollup prose still lists **REQ-001–REQ-022** in some lines; **REQ-023** is covered by **TRC-057** until those summaries are editorially refreshed.

**Next step:** Re-run **`@verifier`** after material code or requirement changes; use **`@implementor`** if a row fails or architecture shifts.
