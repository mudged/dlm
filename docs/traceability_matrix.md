# Traceability matrix

Automated and manual verification recorded by the `@verifier` agent. Column order matches `docs/templates/traceability-matrix-template.md`.

| ID | Functional Requirement | System Component | Test Case Number | Status | Verification |
|----|------------------------|------------------|------------------|--------|--------------|
| TRC-001 | REQ-001 — Go service and Next.js + Tailwind web UI | `backend/cmd/server`, `backend/internal/httpapi`, `backend/internal/config`, `backend/internal/webdist` | `go test ./...` (`internal/httpapi/handlers_test.go`, `internal/config/config_test.go`, `internal/acceptance/requirements_test.go`) | Pass | 2026-03-28: `cd backend && go test ./... -count=1` — all packages ok. |
| TRC-002 | REQ-001 — Go service and Next.js + Tailwind web UI | `web/app/*`, `web/components/*`, `web/next.config.ts`, `scripts/copy-web-export.mjs` | `npm run lint`; `npm run build` (static export to `web/out/`) | Pass | 2026-03-28: `next lint` clean; `next build` with `output: "export"` completed. |
| TRC-003 | REQ-001 — Go service and Next.js + Tailwind web UI | `docs/requirements.md` REQ-001; `docs/acceptance_criteria.md` Feature "Full-stack composition" | AC Gherkin REQ-001 scenarios; `TestAcceptance_REQ001_fullStackComposition` | Pass | 2026-03-28: Doc audit + automated string checks in `internal/acceptance`. |
| TRC-004 | REQ-002 — Responsive, reactive UI | `web/components/ui/Button.tsx`, `StatusRefresh.tsx`, `StatusOnMount.tsx`, `web/app/page.tsx` | AC REQ-002 + `TestAcceptance_REQ002_responsiveAndReactive` | Pass | 2026-03-28: Client `fetch`; touch targets; responsive layout; requirements text checks automated. |
| TRC-005 | REQ-003 — Raspberry Pi 4 Model B | `docs/architecture.md` §1, §3.4, §6; `README.md` | AC REQ-003 scenarios; `TestAcceptance_REQ003_raspberryPiDeployment` | Pass | 2026-03-28: Architecture + README document linux/arm64, Pi 4B, RAM/CPU, single process; requirements checks automated. |
| TRC-006 | REQ-004 — Single executable packaging | `backend/internal/webdist/embed.go`, `backend/cmd/server/main.go`, `backend/internal/httpapi/router.go`, `README.md` | Implementation review vs `docs/architecture.md` §3.4–§3.5, §5–§6 | Pass | 2026-03-28: Single binary embeds UI FS + API; `npm run release:sync` bake step; README documents cross-compile. |
| TRC-007 | REQ-004 — Single executable packaging | `web/next.config.ts`, `web/package.json` | REQ-004 open question (SSR vs static): arch § "Architectural resolution" | Pass | 2026-03-28: `output: "export"`; static export + client hydration; Node at build time only. |
| TRC-008 | REQ-004 + AC | `docs/requirements.md` REQ-004; `docs/acceptance_criteria.md` Feature "Single executable packaging without Docker" | AC Gherkin REQ-004 scenarios; `TestAcceptance_REQ004_singleBinaryNoDocker` | Pass | 2026-03-28: Doc audit + automated requirements text checks. |
| TRC-009 | REQ-001–REQ-017 (acceptance ↔ requirements text) | `backend/internal/acceptance/requirements_test.go` | `go test ./internal/acceptance -count=1` (all `TestAcceptance_*`) | Pass | 2026-04-03: **`TestAcceptance_REQ001`–`REQ007`** plus **`REQ008`**, **`REQ009`**, **`REQ010`** (see **TRC-024**). **Updated 2026-04-06:** suite extended by **`TestAcceptance_REQ011_restLightStateAPI`**, **`TestAcceptance_REQ012_visualizationReflectsLightState`**, **`TestAcceptance_REQ013_modelViewPaginationAndBulkSettings`** (**TRC-029** / **TRC-031** / **TRC-033**), **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`**, **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`** (**TRC-043** / **TRC-044**). **`TestAcceptance_REQ015_scenesCompositeSpacePlacementCRUD`** covers **REQ-015** (see **TRC-050**); **TRC-040** / **TRC-042** remain implementation/architecture gates for scenes. **Updated 2026-04-05:** **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`** covers **REQ-022** text + **`web/package.json`** + editor source (**TRC-056**). **Updated 2026-04-06:** **`TestAcceptance_REQ024_pythonRoutineApiReferenceBelowEditor`**, **`TestAcceptance_REQ027_unifiedRunAndViewportPythonAndShape`** lock **REQ-024** / **REQ-027** text + key UI paths (**TRC-064** / **TRC-017** parity). **Updated 2026-04-06:** **`TestAcceptance_REQ030_pythonSceneRandomHexColour`** locks **REQ-030** + worker/catalog/architecture cross-checks (**TRC-065**). **Updated 2026-04-07:** **`TestAcceptance_REQ021_sceneRoutinesPythonAndSceneAPI`** and **`TestAcceptance_REQ032_threeSeededPythonSampleRoutines`** extend **TRC-017** parity for **REQ-021** / **REQ-032** (**TRC-071** / **TRC-073**). **`TestAcceptance_REQ033_shapeAnimationRoutines`** extends **TRC-017** parity for **REQ-033** (**TRC-074**). |
| TRC-010 | REQ-001, REQ-004, architecture §3.2 (static export + SPA fallback) | `backend/internal/httpapi/router.go` (`staticExportHandler`) | `TestStatic_unknownClientRoute_fallsBackToIndexHTML`, `TestStatic_missingNextAsset_returnsNotFound`, `TestStatic_servesEmbeddableExport`, `TestAPI_precedenceOverStaticPrefix` | Pass | 2026-03-28: Extensionless paths serve `index.html` via `ServeFileFS`; missing `/_next/*` 404; API not swallowed by static handler. |
| TRC-011 | REQ-005 — Wire light model (data shape, CSV, metadata) | `backend/internal/wiremodel/csv.go`; `backend/internal/store/store.go` (schema, `LightDTO` / float coords) | `go test ./internal/wiremodel -count=1`; `go test ./internal/store -count=1` (`TestStore_CreateListGetDelete`, `TestStore_EmptyLights`) | Pass | 2026-04-03: CSV header `id,x,y,z`; ≤1000 rows; contiguous ids 0…n−1; empty model (header-only) allowed; metadata `name` + server `created_at` in DB. |
| TRC-012 | REQ-006 — List, view, delete, create via CSV upload | `backend/internal/httpapi/models.go`, `router.go`; `backend/cmd/server/main.go`; `backend/internal/store` | `TestModels_createValidCSV`, `TestModels_listGetDelete`, `TestModels_duplicateNameConflict` in `internal/httpapi/models_test.go` | Pass | 2026-04-03: `GET/POST /api/v1/models`, `GET/DELETE /api/v1/models/{id}`; multipart `name`+`file`; 201/204/404/409 behaviors exercised. |
| TRC-013 | REQ-006 — List, view, delete, create (UI) | `web/app/models/page.tsx`, `web/app/models/new/page.tsx`, `web/app/models/detail/*`, `web/app/page.tsx` | `npm run lint`; `npm run build` (`output: "export"`); routes `/models`, `/models/new`, `/models/detail` | Pass | 2026-04-03: Client `fetch` to same-origin `/api/v1/models…`; responsive layout patterns; **Note:** detail uses query `?id=` (static export) rather than path `/models/{id}` — equivalent to §4.6 intent. |
| TRC-014 | REQ-007 — Validate CSV on model upload | `backend/internal/wiremodel` (parse/validate); `backend/internal/httpapi/models.go` (`createModel`, `MaxBytesReader`, error envelope) | `TestParseLightsCSV_*` (`csv_test.go`); `TestModels_rejectBadIds`, `TestModels_rejectWrongHeader` (`models_test.go`) | Pass | 2026-04-03: Wrong header, non-contiguous ids, non-numeric coords, >1000 rows → 400 + `validation_failed`; duplicate name → 409; create uses DB transaction (no partial model on failure). |
| TRC-015 | REQ-005–REQ-007 — Config and persistence per architecture §3.3 | `backend/internal/config/config.go` (`DLM_DB_PATH`, `DLM_DATA_DIR`); `store.Open` + `modernc.org/sqlite` | `TestLoad_defaults`, `TestLoad_dlmDBPath` (`config_test.go`); `TestStore_DuplicateName` | Pass | 2026-04-03: Default `data/dlm.db`; SQLite file is runtime state (not embedded); UNIQUE on `models.name`. |
| TRC-016 | REQ-001–REQ-009 — Regression suite (backend) | Full backend + acceptance doc checks | `cd backend && go test ./... -count=1`; `go build ./cmd/server` | Pass | 2026-04-03: `go test ./... -count=1`; `go build -o /tmp/dlm-verify ./cmd/server` — all packages green (post–§3.8 face/surface-even samples). |
| TRC-017 | REQ-001–REQ-017 — Requirements doc string checks (`requirementBlock`) | `backend/internal/acceptance/requirements_test.go` | `go test ./internal/acceptance -count=1`; `TestAcceptance_REQ001` … `TestAcceptance_REQ010`; **`TestAcceptance_REQ011_restLightStateAPI`**; **`TestAcceptance_REQ012_visualizationReflectsLightState`**; **`TestAcceptance_REQ013_modelViewPaginationAndBulkSettings`**; **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`**; **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`**; **`TestAcceptance_REQ021_sceneRoutinesPythonAndSceneAPI`**; **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`**; **`TestAcceptance_REQ024_pythonRoutineApiReferenceBelowEditor`**; **`TestAcceptance_REQ027_unifiedRunAndViewportPythonAndShape`**; **`TestAcceptance_REQ030_pythonSceneRandomHexColour`**; **`TestAcceptance_REQ032_threeSeededPythonSampleRoutines`**; **`TestAcceptance_REQ033_shapeAnimationRoutines`** | Pass | 2026-04-03: REQ-001–010 each have a `TestAcceptance_REQxxx_*` reading **`docs/requirements.md`** (**REQ-008** also reads **`README.md`**). **Updated 2026-04-06:** **TRC-029** / **TRC-033** pattern extended to **REQ-016** / **REQ-017** (**TRC-043** / **TRC-044**). **REQ-015** still lacks `TestAcceptance_REQ015_*` — see **TRC-040** optional gap. **Updated 2026-04-05:** **REQ-022** uses **`requirementBlock`** + **`web/package.json`** + **`PythonRoutineEditorClient.tsx`** checks in **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`** (**TRC-056**). **Updated 2026-04-06:** **REQ-024** / **REQ-027** use the same pattern in **`TestAcceptance_REQ024_*`** / **`TestAcceptance_REQ027_*`** (**TRC-064**). **Updated 2026-04-06:** **REQ-030** uses **`TestAcceptance_REQ030_pythonSceneRandomHexColour`** (**TRC-065**). **Updated 2026-04-07:** **REQ-021** uses **`TestAcceptance_REQ021_sceneRoutinesPythonAndSceneAPI`** (**TRC-071**); **REQ-032** uses **`TestAcceptance_REQ032_threeSeededPythonSampleRoutines`** (**TRC-073**); **REQ-033** uses **`TestAcceptance_REQ033_shapeAnimationRoutines`** (**TRC-074**). |
| TRC-018 | REQ-005 — Acceptance (requirements block) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ005_wireLightModel` | Pass | 2026-04-03: `requirementBlock` scopes ### REQ-005; asserts 1000 cap, CSV columns, metadata, sequential indices. |
| TRC-019 | REQ-006 — Acceptance (requirements block) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ006_modelCRUDAndCSVUpload` | Pass | 2026-04-03: Asserts list/view/delete/upload/CSV and mobile/tablet/desktop UX notes. |
| TRC-020 | REQ-007 — Acceptance (requirements block) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ007_validateCSVOnUpload` | Pass | 2026-04-03: Asserts validation, sequential ids, 1000 cap, finite coords, actionable feedback, no partial persist. |
| TRC-021 | REQ-008 — Single command to build and run locally | `scripts/run.sh`; `README.md` (exact `./scripts/run.sh`, `DLM_SKIP_NPM_CI`); `AGENTS.md` | Implementation review vs `docs/architecture.md` §3.7; `go test ./... -count=1` (no regression) | Pass | 2026-04-03: Script uses `set -euo pipefail`, `web/` then `release:sync`, `backend/` + `exec go run ./cmd/server`; skips `npm ci` when `web/node_modules` exists; honours `DLM_SKIP_NPM_CI=1` (skip) and `DLM_FORCE_NPM_CI=1` (force) — see `scripts/run.sh` and `README.md`. README documents invocation and Windows note. |
| TRC-022 | REQ-009 — Default sample models (sphere, cube, cone) | `backend/internal/samples/*` — **`geom.go`** chord helpers; **`cube.go`** six **face serpentines** + **`appendCubeBridgePoints`** axis-aligned **surface** bridges; **`sphere.go`** **Fibonacci** sphere + greedy **NN** order + **`subdivideGreatCircleArc`**; **`cone.go`** **area-split** **`coneBaseSpiral`** + **`coneRingAtZ`** lateral stack + ruling to **apex** + **`trimConeChain`**; **`store.SeedDefaultSamples`**; **`README.md`** (Default samples) | `TestSphereLights_surfaceChordCount`, `TestCubeLights_surfaceChordCount`, `TestConeLights_surfaceChordCount`; `TestStore_SeedDefaultSamples_idempotent` | Pass | 2026-04-03: `go test ./internal/samples -count=1` — **500 ≤ n ≤ 1000** per shape; chord band ± **`SpatialTestTol`**; sphere on **‖p‖=R** + hemisphere bound; cube **six faces** + **85%** loose-interior quota; cone **lateral + base**; store seed idempotent. |
| TRC-023 | REQ-010 — Three.js visualization when viewing a model | `web/package.json` (direct `three`); `web/components/ModelLightsCanvas.tsx`; `web/lib/wireSegments.ts`, `web/lib/lightBounds.ts`; `web/app/models/detail/ModelDetailClient.tsx` (`next/dynamic` `ssr: false`, inspection copy) | AC Feature "Three.js model view (REQ-010)" (incl. **diameter 0.02 m (radius 0.01 m)** spheres, consecutive segments, hover/touch); `npm run test`; `npm run lint`; `npm run build` | Pass | 2026-04-03: `go test ./... -count=1`; `go build ./cmd/server`; `npm run test && npm run lint && npm run build` (`web/`) — green; **`InstancedMesh.count = n`** for all API lights (no LOD); §4.7 / §8.6 (see REQ-010 notes). Re-verified same day after REQ-009 sample geometry update (no UI code change required). |
| TRC-024 | REQ-008, REQ-009, REQ-010 — Requirements block + README (REQ-008) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ008_singleCommandBuildAndRun`; `TestAcceptance_REQ009_defaultSampleModels`; `TestAcceptance_REQ010_threeJsModelView` | Pass | 2026-04-03: `go test ./internal/acceptance -count=1`; asserts `requirementBlock` text for samples spacing/counts/surface, three.js visualization rules, and README mentions **`scripts/run.sh`** + Go workflow. |
| TRC-025 | REQ-009 — Face/surface-even placement vs architecture §3.8 | `backend/internal/samples/samples_test.go` (`assertSphereHemisphereBound`, `assertCubeFaceQuotas` loose/strict interior, `assertConeLateralBaseSplit`) | Same as TRC-022 sample tests (extended in-file assertions) | Pass | 2026-04-03: Executed with `go test ./internal/samples -count=1`; no hemisphere strip dominance (**≤56%** over **48** sample directions); cube **six** nominal faces + **≥85%** loose interior + strict per-face floor / spread; cone **≥80** base-plane + **≥350** lateral hits. |
| TRC-026 | REQ-011 — REST API for per-light state (query and control) | `backend/internal/store/store.go` (`ListLightStates`, `GetLightState`, `PatchLightState`, `ValidateColor`, `ValidateBrightnessPct`, schema + `"on"` column); `backend/internal/httpapi/lightstate.go`, `router.go` (route order per §3.2) | `TestStore_LightStateListPatch` (`store_test.go`); `TestModels_lightStateEndpoints` (`models_test.go`); AC Feature "Per-light state REST API (REQ-011)" (doc scenarios) | Pass | 2026-04-04: `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify ./cmd/server`. API: `GET …/lights/state`, `GET …/lights/{id}/state`, `PATCH …/lights/{id}/state`; `GET /models/{id}` embeds `on`/`color`/`brightness_pct`; defaults **on=false**, `#ffffff`, **100%** brightness; see TRC-038 for authoritative behaviour; invalid light id → **404**; merged PATCH + **200** body matches architecture §3.9. |
| TRC-027 | REQ-012 — Model visualization reflects per-light state | `web/components/ModelLightsCanvas.tsx` (dual **`InstancedMesh`**: **on** = filled + **`instanceColor`** from `lightAppearance`; **off** = **wireframe** + **transparent**; **`Raycaster.intersectObjects`** on both); `web/lib/lightAppearance.ts`; `web/app/models/detail/ModelDetailClient.tsx` (**Apply** → **PATCH** + **`setModel`** merge); `web/lib/models.ts` **`Light`** shape | `lib/lightAppearance.test.ts` (Vitest); `npm run lint`; `npm run build` (`output: "export"`); AC Feature "Visualization reflects light state (REQ-012)" (doc scenarios) | Pass | 2026-04-04: `cd web && npm run lint && npm test && npm run build` — green. Matches **`docs/architecture.md` §4.7**: brightness scales linear RGB; tooltip adds **on**/**color**/**%**; segments unchanged; same-session refresh via React state after **PATCH** (§4.7 state sync). **Note:** optional **≤5 s** multi-client poll not implemented (architecture: optional). |
| TRC-028 | REQ-011 + REQ-012 — Regression / acceptance alignment | Full stack; `docs/acceptance_criteria.md` REQ-011/012 Features | `go test ./... -count=1`; `npm test`; architecture §3.2/§3.9/§4.7/§8.5/§8.7 audit | Pass | 2026-04-04: No boundary violations vs §7 (single Go process + SQLite); API still registered before static/unknown API paths. **Re-verified 2026-04-04:** `TestAcceptance_REQ011_restLightStateAPI` and `TestAcceptance_REQ012_visualizationReflectsLightState` added — see **TRC-029** (prior follow-up closed). |
| TRC-029 | REQ-011 + REQ-012 — Requirements block automation (TRC-017 extension) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ011_restLightStateAPI`; `TestAcceptance_REQ012_visualizationReflectsLightState`; `go test ./internal/acceptance -count=1` | Pass | 2026-04-04: `requirementBlock` asserts REST, per-light state, hex/brightness/PATCH/persist/architecture refs (**REQ-011**); filled/hollow/semi-transparent, REQ-011/010/002 ties, no-reload sync, **diameter 0.02 m (radius 0.01 m)** (**REQ-012**). Full suite: `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify ./cmd/server`; `cd web && npm run lint && npm test` — all green. |
| TRC-030 | REQ-013 — Batch light state API (architecture §3.10) | `backend/internal/store/store.go` (`BatchPatchLightStates`, `ErrBatchEmptyIDs`, `ErrBatchDuplicateIDs`); `backend/internal/httpapi/lightstate.go` (`patchLightStatesBatch`, **64 KiB** `MaxBytesReader`); `backend/internal/httpapi/router.go` (`PATCH …/lights/state/batch` before per-`lightId` routes) | `TestStore_BatchPatchLightStates` (`store_test.go`); `TestModels_lightStateEndpoints` batch **PATCH** assertion (`models_test.go`); AC Feature "Model view light list pagination and bulk settings (REQ-013)" (API-related scenarios via integration) | Pass | 2026-04-04: `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify ./cmd/server`. **Audit:** single **SQLite** transaction; **200** `{ "states": [...] }` sorted by **`id`**; **400** empty/duplicate/out-of-range **`ids`** and validation failures; **404** unknown model — matches **`docs/architecture.md` §3.2 / §3.10 / §8.8**. **Re-verified 2026-04-04:** `go test ./... -count=1`; `go build -o /tmp/dlm-verify-2 ./cmd/server`. |
| TRC-031 | REQ-013 — Paginated light list, go-to-id, multi-select, bulk apply (architecture §4.8) | `web/app/models/detail/ModelDetailClient.tsx` (`PAGE_SIZE_OPTIONS` **25/50/100**, default **50**; client-side paging over **`GET /models/{id}`**; **Go to light id** + row **`scrollIntoView`**; checkboxes, **select page** + **indeterminate** header; **Shift+click** range on current page; cross-page **`selectedIds`** + **Clear selection**; **`BulkLightStatePanel`** → **`/lights/state/batch`**; **`mergeBatchLightStatesApi`** updates canvas props) | `TestAcceptance_REQ013_modelViewPaginationAndBulkSettings` (`internal/acceptance/requirements_test.go`); `npm run test` (Vitest); `npm run build` (`output: "export"`); AC Feature "Model view light list pagination and bulk settings (REQ-013)" (UI scenarios — doc-to-code audit) | Pass | 2026-04-04: `go test ./internal/acceptance -run TestAcceptance_REQ013 -count=1`; `cd web && npm run test && npm run build` — green. **Audit:** pagination chrome hidden when **`lightCount ≤ 1`** per §4.8 trivial case; bulk + per-row **Apply** coexist; no **hover-only** essential actions; aligns **REQ-002** touch targets (**Button** / inputs). **Re-verified 2026-04-04:** `npm run test`; `npm run build`. |
| TRC-032 | REQ-011 + REQ-012 + REQ-013 — Regression (boundaries unchanged) | Full stack per **`docs/architecture.md` §7** | `go test ./... -count=1`; `go build ./cmd/server`; `npm run test`; `npm run build` | Pass | 2026-04-04: Re-confirmed **one** Go process + API-before-static registration; batch route does not collide with **`GET …/lights/{lightId}/state`**; client **`fetch`** same-origin **`/api/v1/...`**; **§4.7** state sync text covers batch **PATCH** — **`ModelLightsCanvas`** receives updated **`lights`** after bulk merge (**REQ-012** timeliness). **Re-verified 2026-04-04:** same commands — all green. |
| TRC-033 | REQ-013 — Requirements block automation (TRC-017 / TRC-029 extension) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ013_modelViewPaginationAndBulkSettings`; `go test ./internal/acceptance -count=1` | Pass | 2026-04-04: `requirementBlock` asserts **`### REQ-013`** pagination, multi-select, three page-size choices, go-to **id** + feedback, **REQ-005**/**006**/**011**/**012**/**002** ties, bulk **apply**, **persist**, responsive notes, **checkbox**, no **hover-only** essentials. Full regression: `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify-2 ./cmd/server`; `cd web && npm run test && npm run build`. |
| TRC-034 | REQ-012 — Visualization reflects per-light state (on-sphere colour + post-PATCH orbit) | `web/components/ModelLightsCanvas.tsx` (**`MeshBasicMaterial`** for **on** **`InstancedMesh`** with **`instanceColor`** + **`colorFromHexAndBrightness`**; optional **`cameraPersistenceKey`** restores **`OrbitControls.target`** and camera position when **`lights`** updates share the same key); `web/app/models/detail/ModelDetailClient.tsx` (`cameraPersistenceKey={model.id}`) | AC Feature "Visualization reflects light state (REQ-012)" (on-light hex + brightness; visualization updates after successful write); `web/lib/lightAppearance.test.ts`; `npm run test`; `npm run lint`; `npm run build`; `go test ./... -count=1` | Pass | 2026-04-04: **Audit vs `docs/architecture.md` §4.7:** **`vertexColors: true`** on **on** material was incorrect with **no** geometry **`color`** attribute (Three.js multiplies **vColor** by missing/zero vertex colour → black spheres). Default **`vertexColors`** preserves **`instanceColor`** path. **`cameraPersistenceKey`** avoids reframing on each **`lights`** identity change after **PATCH** while keeping **§4.7** state sync (client merge → props → scene rebuild). **Regression:** `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify-verifier ./cmd/server`; `cd web && npm run test && npm run lint && npm run build` — all green. **Manual:** confirm sphere tint and orbit after **Apply** in browser. |
| TRC-035 | REQ-014 — Default light state (off, 100% white) and model reset | `backend/internal/store/store.go` (`DefaultLightOn=false`, `ResetAllLightStates`, insert defaults); `backend/internal/httpapi/lightstate.go` (`postResetLightStates`); `backend/internal/httpapi/router.go` (`POST /models/{id}/lights/state/reset`); `web/app/models/detail/ModelDetailClient.tsx` (**Reset lights** + `mergeBatchLightStatesApi`) | AC Feature "Default light state and model reset (REQ-014)"; `TestStore_ResetAllLightStates`; `TestModels_lightStateEndpoints` (POST reset after batch); `go test ./... -count=1` | Pass | 2026-04-04: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify ./cmd/server`**. Reset returns **200** `{ "states": [...] }` for all lights **off** / **`#ffffff`** / **100%**; **404** unknown model. UI button non–hover-only (full width on narrow viewports). |
| TRC-036 | REQ-010 — Three.js view (2 cm spheres; #D0D0D0 segments at 85% transparency) | `web/lib/wireSegments.ts` (`SPHERE_RADIUS_M = 0.01`); `web/components/ModelLightsCanvas.tsx` (`LineBasicMaterial` + **`MeshBasicMaterial`** off layer **`0xd0d0d0`**, **opacity 0.15**); `web/app/models/detail/ModelDetailClient.tsx` | AC Feature "Three.js model view (REQ-010)" (updated diameter + segment colour/transparency); `web/lib/wireSegments.test.ts`; `npm run test`; `npm run lint`; `npm run build`; `TestAcceptance_REQ010_threeJsModelView` | Pass | 2026-04-04: **`cd web && npm run test && npm run lint && npm run build`**; **`cd backend && go test ./internal/acceptance -run TestAcceptance_REQ010 -count=1`**. Consecutive **id** segments only (**`buildWireSegmentPositions`**). **Supersedes** TRC-023 historical geometry / white segment-line narrative for current product behaviour. **Updated 2026-04-04:** softer **`#D0D0D0`** + **15%** opacity per **`docs/requirements.md`**. |
| TRC-037 | REQ-012 — Off lights match segment styling (#D0D0D0, 85% transparency) | `web/components/ModelLightsCanvas.tsx` (**off** = filled **`MeshBasicMaterial`**, not wireframe; same grey/opacity as wire lines); `web/lib/lightAppearance.ts` (unchanged **on** colour × brightness) | AC Feature "Visualization reflects light state (REQ-012)"; `TestAcceptance_REQ012_visualizationReflectsLightState`; `npm run test`; `npm run build` | Pass | 2026-04-04: Doc automation + **`npm run test`/`build`** green. **§4.7** state sync extended in code comments/effect deps to include **`POST …/reset`** (client merge). **Supersedes** TRC-027/034 **wireframe-off** narrative where it conflicts with current REQ-012 text. **Updated 2026-04-04:** **`#D0D0D0`** / **opacity 0.15** aligned with REQ-010. |
| TRC-038 | REQ-011 — Per-light REST + defaults tied to REQ-014 | `backend/internal/store/store.go` (`DefaultLightOn=false`, schema **`"on"` DEFAULT 0** on new/`ALTER`); `backend/internal/httpapi/lightstate.go` | `TestStore_CreateListGetDelete`, `TestStore_LightStateListPatch`, `TestStore_BatchPatchLightStates`, `TestModels_lightStateEndpoints` (initial **GET …/lights/state** both **off**); `TestAcceptance_REQ011_restLightStateAPI` | Pass | 2026-04-04: **`go test ./... -count=1`**. **TRC-026** historical default wording is **obsolete**; this row is authoritative for **off** default + **#ffffff** + **100%** brightness. |
| TRC-039 | REQ-005 — Chain adjacency (at most two neighbours by consecutive id) | `docs/requirements.md` REQ-005 rule 6; `web/lib/wireSegments.ts` **`buildWireSegmentPositions`** (sorted **id**, edges **(i,i+1)** only); `ModelLightsCanvas` segment geometry | AC Feature "Wire light model definition" (REQ-005 chain scenario); implementation review vs **`docs/architecture.md` §3.6** chain note | Pass | 2026-04-04: No extra edges in segment builder; **n ≤ 1** → no segments. |
| TRC-040 | REQ-015 — Scenes: composite space, placement, CRUD, and three.js view | `backend/internal/store/store.go` (`ensureSceneTables`, `ensureSceneModelOrdinal`, `Delete` pre-check); `backend/internal/store/scenes.go` (`CreateScene` with ordered `modelIDs`, `OffsetsForFirstSceneModel`, `DefaultOffsetsForNewModel`, `scene_models.ordinal`); `backend/internal/httpapi/scenes.go` (`POST /scenes` JSON **`model_id` only**, `DisallowUnknownFields`); `json.go` (`writeAPIErrorDetail`); `backend/internal/httpapi/router.go`; `web/lib/scenes.ts`; `web/components/SceneLightsCanvas.tsx`; `web/app/scenes/*` | `TestScenes_CreateGetDelete`, `TestScenes_CreateSceneAutoOffsetsNegativeModelCoords`, `TestScenes_CreateScenePreservesModelOrder`, `TestScenes_LastModelRemoval` (`store/scenes_test.go`); `TestScenes_createAndGet`, `TestScenes_createRejectsClientOffsets` (`httpapi/scenes_test.go`); AC Feature "Scenes composite 3D space and management (REQ-015)" | Pass | 2026-04-04 (re-verify): `cd backend && go test ./... -count=1`; `go build -o /tmp/dlm-verify-verifier ./cmd/server`; `cd web && npm run lint && npm run test && npm run build`. **§3.12–§3.13:** server-computed integer offsets on create; **400** if client sends offset fields; canonical **`lights`** unchanged; **`GET` scene** exposes **`x,y,z`** + **`sx,sy,sz`**; **`scene_models.ordinal`** preserves **JSON** array order; **`+X`** placement for models after the first; **`scene_last_model`** **409**; **`GetScene`** closes **`rows`** before nested loads. **§4.9:** `/scenes/new` — name + ordered models (Up/Down), no create-time offset inputs. |
| TRC-041 | REQ-006 — Model delete blocked when model is used in scenes (rule 5) | `backend/internal/store/store.go` (`Delete` → **`ModelInUseError`** + **`listScenesReferencingModel`**); `backend/internal/httpapi/models.go` (`deleteModel` → **409** **`model_in_scenes`** + **`details.scenes`**); `web/app/models/page.tsx` (409 message + scene names) | `TestModels_delete409WhenInScene` (`httpapi/scenes_test.go`); AC Feature "Model management" scenario "REQ-006 blocks model delete when model is used in scenes and explains why" | Pass | 2026-04-04 (re-verify): Same regression commands as **TRC-040** row above; **409** body includes **`error.code`** **`model_in_scenes`** and **`details.scenes`** with **`id`**/**`name`**. **TRC-012** remains valid for core CRUD; this row records the **REQ-015** interaction. |
| TRC-042 | REQ-015 — Architecture alignment: auto placement + create payload (post–§3.12 revision) | `docs/architecture.md` §3.12–§3.13, §4.9, §8.10; implementation in **TRC-040** components | Doc-to-code audit (no new automated test); regression: `go test ./... -count=1`; `npm run build` | Pass | 2026-04-04: **`@verifier`** audit — **`POST /api/v1/scenes`** matches architecture (**ordered** **`model_id`** list; server placement; reject extra keys per element); **`CreateScene`** matches create-time algorithm; **`/scenes/new`** omits offset fields. **REQ-015 acceptance parity:** **`TestAcceptance_REQ015_scenesCompositeSpacePlacementCRUD`** is recorded in **TRC-050**; **TRC-040** integration tests + AC review remain implementation gates. |
| TRC-043 | REQ-016 — Camera reset for model and scene 3D views | `web/components/ModelLightsCanvas.tsx`, `web/components/SceneLightsCanvas.tsx` (`cameraResetVersion` forces default orbit vs `cameraPersistenceKey` restore); `web/app/models/detail/ModelDetailClient.tsx`, `web/app/scenes/detail/SceneDetailClient.tsx` (**Reset camera** `Button`) | **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`** (`internal/acceptance/requirements_test.go`); AC Feature "Camera reset for 3D views (REQ-016)"; `npm run lint`; `npm run test`; `npm run build` (`/models/detail`, `/scenes/detail` export) | Pass | 2026-04-04: **`cd web && npm run lint && npm test && npm run build`** — green. **Audit vs `docs/architecture.md` §4.7 / §4.9 / §8.14:** reset is **client-only** (no `fetch`); default framing matches initial **`boundingFromLights`** + **`OrbitControls.target`** + camera offset recipe; **touch**/**full-width** buttons on narrow viewports (**REQ-002**). **Updated 2026-04-06:** **`requirementBlock("REQ-016")`** assertions in **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`**; **`go test ./internal/acceptance -run TestAcceptance_REQ016 -count=1`** — Pass. **Manual:** orbit after pan/zoom then **Reset camera** in browser. |
| TRC-044 | REQ-017 — Options: factory reset with confirmation | `backend/internal/store/store.go` (`seedThreeCanonicalSamplesTx`, `FactoryReset`, `SeedDefaultSamples` refactor); `backend/internal/httpapi/system.go`; `backend/internal/httpapi/router.go` (**`POST /system/factory-reset`** registered first on API mux); `web/app/options/page.tsx` (`<dialog>` + **Cancel** / **Erase everything**); `web/app/models/page.tsx` (**`Suspense`** + **`factoryReset=1`** success banner + `router.replace`) | **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`** (`internal/acceptance/requirements_test.go`); `TestStore_FactoryReset` (`store_test.go`); `TestAPIv1FactoryReset_resetsToThreeSamples` (`handlers_test.go`); AC Feature "Options factory reset with confirmation (REQ-017)" | Pass | 2026-04-04: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-pass ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`**. **Audit vs §3.14 / §4.10 / §8.13:** single **SQLite** transaction; routine runs and routines are deleted before scenes/models per FK constraints (see **TRC-055** / **TRC-070**), then seed; **200** `{ "ok": true }`; modal copy warns **permanent** loss and **three samples** only; **unauthenticated** endpoint noted in architecture §9 (operator LAN exposure). **Route ordering:** **`/system/factory-reset`** not shadowed by **`GET /{path...}`**. **Updated 2026-04-06:** **`requirementBlock("REQ-017")`** assertions in **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`**; **`go test ./internal/acceptance -run TestAcceptance_REQ017 -count=1`** — Pass. |
| TRC-045 | REQ-018 — Application shell: themes, collapsible navigation, branding, and Font Awesome controls | `web/app/layout.tsx` (inline **`dlm-theme`** script + **`AppShell`**); `web/components/AppShell.tsx` (**burger**, **`faLightbulb`** regular + title **`Domestic Light & Magic`**, **`faSun`/`faMoon`** toggle, left **`aside`** nav **`/`** / **`/models`** / **`/scenes`** / **`/options`**, **`localStorage`** **`dlm-theme`**, mobile drawer + desktop collapse); `web/components/UiThemeContext.tsx`; `web/tailwind.config.ts` (**`darkMode: 'class'`**); `web/lib/theme.ts`; `web/lib/theme.test.ts`; `web/components/ui/Button.tsx`, `web/components/ui/ButtonLink.tsx`; `web/components/ModelLightsCanvas.tsx`, `web/components/SceneLightsCanvas.tsx` (**`isDarkDom`** / **`uiTheme`** deps); page clients under **`web/app/**`** ( **`Button`** **`icon=`** usage) | AC Feature "Application shell themes navigation branding and Font Awesome (REQ-018)" (doc-to-code audit); **`npm run test`** (**`lib/theme.test.ts`**); **`npm run lint`**; **`npm run build`** (`output: "export"`); **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req018 ./cmd/server`** (regression) | Pass | 2026-04-04: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req018 ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`** — all **Pass**. **Audit vs `docs/architecture.md` §4.11:** class-based **Tailwind** **`dark:`** on **`html`**; default **light** + persist **`dlm-theme`**; shell **white** / **gray-900** surfaces; **Font Awesome** **SVG** packages + **`@fortawesome/react-fontawesome`**; **REQ-010**/**012** canvases follow **`html.dark`**. **Optional gap:** no **`TestAcceptance_REQ018_*`** in **`internal/acceptance`** yet (**TRC-017** parity); gate remains **TRC-045** audit + **`theme.test.ts`** + **`npm run build`**. **Boundaries:** verifier made **no** application source edits. **Superseded for theme default:** see **TRC-047** ( **`prefers-color-scheme`** until **`dlm-theme`** set). **Superseded for 3D backdrop:** see **TRC-046** (**REQ-019** fixed grey; canvases no longer track shell via **`isDarkDom`**). |
| TRC-046 | REQ-019 — Three.js model and scene views: fixed dark-grey viewport | `web/lib/vizViewport.ts` (**`VIZ_VIEWPORT_BG`**, **`VIZ_VIEWPORT_BG_CSS`**); `web/components/ModelLightsCanvas.tsx`, `web/components/SceneLightsCanvas.tsx` (**`scene.background`**, **`renderer.setClearColor`**, grid helper tuned for dark viz, wrapper **`backgroundColor`** **`#262626`**, empty-state scrim); `docs/architecture.md` §4.7 / §4.9 | AC Feature "Three.js fixed dark-grey viewport (REQ-019)" (doc-to-code audit); **`web/lib/vizViewport.test.ts`**; **`npm run test`**; **`npm run lint`**; **`npm run build`**; **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-20260404 ./cmd/server`** | Pass | 2026-04-04: Full regression green. **Audit vs architecture:** WebGL clear and scene fill use **fixed** **`0x262626`** independent of **`html.dark`**; letterbox wrapper matches **CSS** hex; grid opacity/colours aligned to dark viz (**§4.7**). **Optional gap:** no **`TestAcceptance_REQ019_*`** in **`internal/acceptance`** — AC + **TRC-046** audit + **`vizViewport.test.ts`** gate. Verifier made **no** application source edits. |
| TRC-047 | REQ-018 — Default theme follows system preference until user overrides | `web/lib/theme.ts` (**`resolveInitialTheme`**); `web/app/layout.tsx` (inline script: valid **`dlm-theme`** wins; else **`matchMedia('(prefers-color-scheme: dark)')`**); `web/components/AppShell.tsx` (**`useLayoutEffect`** sync); `web/lib/theme.test.ts` (stored vs system vs **`matchMedia`** throw) | AC Feature REQ-018 scenario "defaults theme to system preference until user overrides"; **`TestAcceptance_REQ018_applicationShellThemesNavAndFontAwesome`** (requirements block); **`npm run test`** (**`theme.test.ts`**); **`npm run lint`**; **`npm run build`**; **`go test ./... -count=1`** | Pass | 2026-04-04: Same regression commands as **TRC-046** row — all **Pass**. **Audit vs `docs/architecture.md` §4.11 / §8.1:** first paint script + **`resolveInitialTheme`** match **REQ-018** business rule 1. Verifier made **no** application source edits. |
| TRC-048 | REQ-020 — Scene spatial API for dimensions, filtered retrieval, and bulk updates | `backend/internal/httpapi/router.go`, `backend/internal/httpapi/scenes.go`, `backend/internal/store/scenes.go` | `TestScenes_SpatialQueriesAndDimensions`, `TestScenes_SpatialBulkUpdateAndInvalidGeometry` (`internal/store/scenes_test.go`); `TestScenes_spatialEndpointsDimensionsAndQueries`, `TestScenes_spatialBulkUpdateAndInvalidGeometry` (`internal/httpapi/scenes_test.go`); `cd backend && go test ./internal/store -run "TestScenes_Spatial" -count=1`; `cd backend && go test ./internal/httpapi -run "TestScenes_spatial" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Verifier audit vs `docs/architecture.md` §3.15 / §8.15 found no boundary violations. New routes are registered on `/api/v1/scenes/{id}/...` for dimensions, full lights, cuboid/sphere query, and cuboid/sphere bulk state updates. Store enforces finite + positive geometry validation (`ErrSceneInvalidGeometry`), inclusive cuboid/sphere containment, scene-space matching (`sx/sy/sz`), and transactional bulk updates with rollback semantics (no partial writes). Tests confirm dimensions payload shape, scene-coordinate retrieval/filtering, bulk update behavior, invalid-geometry rejection (`validation_failed`), and canonical model coordinates remaining unchanged. |
| TRC-049 | REQ-014 — Default light state (off, 100% white) and model reset (requirements block automation) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ014_defaultLightStateAndReset`; `cd backend && go test ./internal/acceptance -run "TestAcceptance_REQ014" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Added dedicated `requirementBlock("REQ-014")` assertions for default off/white/100 state, reset control semantics, responsive/non-hover requirements, and REQ dependency links. |
| TRC-050 | REQ-015 — Scenes: composite space, placement, CRUD, and three.js view (requirements block automation) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ015_scenesCompositeSpacePlacementCRUD`; `cd backend && go test ./internal/acceptance -run "TestAcceptance_REQ015" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Added dedicated REQ-015 requirements assertions covering create/list/delete/open scene lifecycle, automatic integer placement on create, canonical-vs-derived coordinate separation, non-negative containment, +X add-model default, last-model confirmation flow, and required dependencies. |
| TRC-051 | REQ-019 — Three.js model and scene views: fixed dark-grey viewport (requirements block automation) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ019_fixedDarkGreyThreeJSViewport`; `cd backend && go test ./internal/acceptance -run "TestAcceptance_REQ019" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Added dedicated REQ-019 requirements assertions for fixed dark-grey viewport policy across shell themes, REQ-018/010/015/002 linkage, and responsive/non-hover constraints. |
| TRC-052 | REQ-020 — Scene spatial API for dimensions, filtered retrieval, and bulk updates (requirements block automation) | `backend/internal/acceptance/requirements_test.go` | `TestAcceptance_REQ020_sceneSpatialAPI`; `cd backend && go test ./internal/acceptance -run "TestAcceptance_REQ020" -count=1`; `cd backend && go test ./... -count=1` | Pass | 2026-04-05: Added dedicated REQ-020 requirements assertions for dimensions endpoint, all-lights retrieval in scene coordinates, cuboid/sphere query + bulk update operations, REQ-011 state semantics, canonical coordinate immutability, invalid-geometry rejection, and dependency references. |
| TRC-053 | REQ-020 — Scene whole-scene uniform patch and per-light batch patch (`PATCH …/lights/state/scene`, `PATCH …/lights/state/batch`) | `backend/internal/store/scenes.go` (`PatchSceneLightsScene`, `PatchSceneLightsBatch`); `backend/internal/httpapi/scenes.go`, `router.go` | `TestPatchSceneLightsSceneAndBatch`, `TestPatchSceneLightsBatchRejectsForeignLight` (`internal/store/scenes_test.go`); `go test ./... -count=1` | Pass | 2026-04-05: Audit vs `docs/architecture.md` §3.15 — routes and transactional store paths; batch membership validation; 4 MiB body cap; canonical `lights` coords unchanged. |
| TRC-054 | REQ-021 — Scenes: routines (definitions, run/stop, first random-colour type) | `backend/internal/store/routines.go`; `backend/internal/store/store.go`; `backend/internal/httpapi/routines.go`, `router.go`; `backend/cmd/server/main.go`; `web/lib/routines.ts`, `web/app/routines/page.tsx`, `web/app/scenes/detail/SceneDetailClient.tsx`, `web/components/AppShell.tsx` | `TestRoutinesCreateStartStopDelete` (`internal/store/scenes_test.go`); **`TestAcceptance_REQ021_sceneRoutinesPythonAndSceneAPI`**; AC Feature "Scene routines definitions run stop and first random colour type (REQ-021)"; `go test ./... -count=1`; `npm run lint`; `npm test`; `npm run build` | Pass | 2026-04-05: Audit vs §3.14–§3.16, §6.2, §7 DB label. **Updated 2026-04-07:** **`TestAcceptance_REQ021_*`** closes prior **TRC-017** optional gap; **TRC-071** is authoritative for **no** Go-side routine ticker (**`routine_scheduler.go`** obsolete). |
| TRC-055 | REQ-017 — Factory reset removes routines and routine runs | `backend/internal/store/store.go` (`FactoryReset` delete order) | `TestStore_FactoryReset`; `TestAPIv1FactoryReset_resetsToThreeSamples`; `go test ./... -count=1` | Pass | 2026-04-05: Confirms `routine_runs` then `routines` deleted before scenes in same transaction; supplements **TRC-044** where narrative still says scenes-first only. |
| TRC-056 | REQ-022 — Python scene routines: editor, Pyodide worker, scene API, persistence, and forced stop | `backend/internal/store/routines.go`, `store.go`; `backend/internal/httpapi/routines.go`, `routine_scheduler.go`, `router.go`; `web/public/dlm-python-scene-worker.mjs`, **`web/public/dlm-python-editor-worker.mjs`**; **`web/lib/pythonEditorWorker.ts`**, **`web/lib/pythonRoutineCodemirror.ts`**; `web/components/PythonRoutineHost.tsx`; `web/app/routines/python/PythonRoutineEditorClient.tsx`, `page.tsx`; `web/app/scenes/detail/SceneDetailClient.tsx`; `web/lib/routines.ts`; `web/app/routines/page.tsx`; **`docs/architecture.md`** §3.17 / §4.13 (**CodeMirror** **6** **only**) | AC Feature "Python scene routines editor API docs and execution (REQ-022)" (doc review); `internal/store/routines_python_test.go`; **`lib/pythonRoutineCodemirror.test.ts`**; **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`**; `go test ./internal/acceptance -run TestAcceptance_REQ022 -count=1`; `cd backend && go test ./... -count=1`; `cd web && npm run lint && npm test && npm run build` | Pass | 2026-04-05: **`go test ./... -count=1`**; **`npm run lint`**, **`npm test`**, **`npm run build`** — all **Pass**. **Audit vs §3.17 / §4.13 / REQ-022:** **`PythonRoutineHost`** **`T_FORCE_MS` = **5000**; **editor** **CodeMirror** **6** **only** (**no** **Monaco** **in** **`web/package.json`**; **`@uiw/react-codemirror`** + **`@codemirror/*`** **^6**); **`lintGutter`** + **Pyodide** **`ast.parse`** (**`dlm-python-editor-worker.mjs`**); **`scene.`** completions; **format-on-save** (**`black`** / **`textwrap`**). **`TestAcceptance_REQ022_*`** locks **REQ-022** **text**, **deps**, **and** **`PythonRoutineEditorClient.tsx`**. **Optional gap:** no dedicated **`internal/httpapi`** routine **GET/PATCH** tests (store tests + regression remain the gate). |
| TRC-057 | REQ-023 — Create routine: single type dropdown (includes Python) | `web/app/routines/new/RoutineNewClient.tsx`, `web/app/routines/new/page.tsx`, `web/app/routines/page.tsx`, `web/app/routines/python/PythonRoutineEditorClient.tsx`, `web/app/routines/python/new/page.tsx`, `web/lib/routines.ts` (`ROUTINE_TYPE_CREATE_OPTIONS`, `isCreatableRoutineType`) | AC Feature "Create routine type selection dropdown (REQ-023)" (Gherkin scenarios reference **`docs/requirements.md`**); **`web/lib/routines.test.ts`** (**`routine types (REQ-023)`**); `cd backend && go test ./... -count=1`; `cd web && npm run lint && npm test && npm run build` | Pass | 2026-04-05: **Audit vs `docs/architecture.md` §4.11–§4.13:** **`/routines/new`** hosts labeled **`type`** **`<select>`** with **`random_colour_cycle_all`** and **`python_scene_script`**; **`/routines`** has a single **New routine** action (no standalone primary **Python-only** create button); **`/routines/python/new`** **`redirect`** → **`/routines/new?type=python_scene_script`**; **`PythonRoutineEditorClient`** without **`id`** **`replace`**s to the same; **`AppShell`** **`NAV`** lists **`/routines`** only (no Python-only create nav item). **`POST /api/v1/routines`** unchanged (client sends **`type`** + **`python_source`** when Python). **Optional gap:** no **`TestAcceptance_REQ023_*`** in **`internal/acceptance`** — gate is **TRC-057** + **`routines.test.ts`** + doc AC review. **Supplements** **TRC-054** (REQ-021 routine list/run UX; create path now REQ-023). Verifier made **traceability-only** edits. |
| TRC-058 | REQ-024 — Python routine view: bottom API reference with descriptions and snippets | `web/lib/pythonSceneApiCatalog.ts` (`SCENE_API_CATALOG`), `web/components/PythonSceneApiCatalogSection.tsx`, `web/app/routines/python/PythonRoutineEditorClient.tsx` (bottom **`#python-scene-api-catalog`**, mobile link **Full API ↓**) | AC Feature "Python routine bottom API catalog (REQ-024)" (Gherkin in **`docs/acceptance_criteria.md`**); **`web/lib/pythonSceneApiCatalog.test.ts`**; **`TestAcceptance_REQ024_pythonRoutineApiReferenceBelowEditor`**; `cd web && npm run test && npm run build` | Pass | 2026-04-06: **Audit vs `docs/architecture.md` §4.13:** catalog lists every **`scene`** property/method with description + **`<pre>`** snippet; rendered after primary workflow + editor/help in DOM order. Full regression: **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test && npm run build`** — Pass. **Updated 2026-04-06:** **`TestAcceptance_REQ024_pythonRoutineApiReferenceBelowEditor`** in **`internal/acceptance`** locks **`requirementBlock("REQ-024")`** + catalog/editor wiring (**TRC-017** / **TRC-064**). Verifier made **traceability-only** edits. |
| TRC-059 | REQ-025 — Python routine default code: sphere interior colour changes | `web/lib/pythonSceneApiCatalog.ts` (`PYTHON_ROUTINE_DEFAULT_SOURCE`), `web/app/routines/new/RoutineNewClient.tsx`, `web/app/routines/python/PythonRoutineEditorClient.tsx` (**Reset to template** + load fallback) | AC Feature "Python routine default sphere colour template (REQ-025)"; **`pythonSceneApiCatalog.test.ts`** (asserts **`set_lights_in_sphere`** + **`await`**); `npm test` | Pass | 2026-04-06: New **`python_scene_script`** rows receive default template on **`POST`**; editor reset-with-confirm restores same string. **Audit vs §4.13** — sphere bulk state via **`set_lights_in_sphere`**. Same regression commands as **TRC-058** — Pass. **Optional gap:** no **`TestAcceptance_REQ025_*`**. Verifier made **traceability-only** edits. |
| TRC-060 | REQ-026 — Python scene binding: width, depth, and height | `web/public/dlm-python-scene-worker.mjs` (**`scene.width`**, **`scene.height`**, **`scene.depth`** from **`dimsCache.size`**); `web/lib/pythonSceneApiCatalog.ts` (docs + completions source); `web/lib/pythonRoutineCodemirror.ts` | AC Feature "Python scene binding width depth height (REQ-026)"; **`pythonSceneApiCatalog.test.ts`** + **`pythonRoutineCodemirror.test.ts`** (**width**/**depth** in completion labels); audit vs **`docs/architecture.md` §3.15** / **§3.17** | Pass | 2026-04-06: Worker getters match dimensions **`size`** object; catalog documents +X/+Y/+Z mapping per architecture. Regression suite Pass. **Optional gap:** no **`TestAcceptance_REQ026_*`**. Verifier made **traceability-only** edits. |
| TRC-061 | REQ-027 — Routine authoring: unified run-in-scene and live viewport (Python and shape) | `web/app/routines/python/PythonRoutineEditorClient.tsx` + `web/app/routines/shape/ShapeRoutineEditorClient.tsx` (**one** **`targetSceneId`** **`select`**, **`SceneLightsCanvas`**, reset lights + **Reset camera**); `web/lib/scenes.ts` (`patchSceneLightsStateScene`); `web/components/PythonRoutineHost.tsx` + `web/public/dlm-python-scene-worker.mjs` (**`iterationComplete`** → **`onIterationComplete`** refresh); `web/app/scenes/detail/SceneDetailClient.tsx` (**Edit Python** adds **`&scene=`**) | AC Feature "Routine authoring unified run-in-scene and live viewport (REQ-027)"; **`TestAcceptance_REQ027_unifiedRunAndViewportPythonAndShape`**; same regression as **TRC-058**; manual spot-check vs **`docs/architecture.md` §4.13** / **§8.18** | Pass | 2026-04-06: **`PATCH /api/v1/scenes/{id}/lights/state/scene`** with REQ-014 triple for reset lights; **`cameraResetVersion`** for camera only; debug view refetches when active run’s scene matches debug selection (**REQ-012** timeliness class). **Updated 2026-04-09 (REQ-027):** reset scene lights **must** stop an active run for that scene first, then apply defaults (**`docs/requirements.md`** / **`docs/architecture.md` §4.13**). **Superseded for layout:** unified **Run your script** region + single **`targetSceneId`** (**TRC-064**). **Updated 2026-04-06:** **`TestAcceptance_REQ027_unifiedRunAndViewportPythonAndShape`** locks **`requirementBlock("REQ-027")`** + unified viewport wiring (**TRC-017** / **TRC-064**). Verifier made **traceability-only** edits. |
| TRC-062 | REQ-028 — Three.js light spheres: brightness-scaled emissive glow | `web/lib/lightAppearance.ts` (`meshStandardMaterialForOnLight`, `emissiveIntensityFromBrightness`, `emissiveColorFromHex`, `colorFromHexAndBrightness`); `web/lib/vizViewport.ts` (`configureVizWebGLRenderer`); `web/components/ModelLightsCanvas.tsx`, `web/components/SceneLightsCanvas.tsx` (**on** = cached **`MeshStandardMaterial`** per hex+brightness; **off** = **`MeshBasicMaterial`** **`#D0D0D0`** **opacity 0.15**; reduced **Ambient**/**Directional** intensities) | AC Feature "Three.js emissive glow scaled by brightness (REQ-028)" in **`docs/acceptance_criteria.md`**; **`web/lib/lightAppearance.test.ts`**; **`web/lib/vizViewport.test.ts`**; **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test && npm run build`** | Pass | 2026-04-06: **Audit vs `docs/architecture.md` §4.7 REQ-028 / §6.5:** **on** spheres use **emissive** + **`emissiveIntensity`** monotonic in **`brightness_pct`** with strong read at **100%**; renderer **sRGB** + **ACESFilmic** + exposure **0.95**; no mandatory bloom. **Scene** detail and **Python** visual debug inherit via **`SceneLightsCanvas`**. **Supersedes** matrix narratives that **on** spheres were **`MeshBasicMaterial`** + **`instanceColor`** only (**TRC-027** / **TRC-034** historical text). **Optional gap:** no **`TestAcceptance_REQ028_*`** in **`internal/acceptance`**. Verifier made **traceability-only** edits. |
| TRC-063 | REQ-029 — High-throughput light state updates (batching, connections, push) | `backend/internal/httpapi/revision_hub.go`; `router.go` (**`GET …/lights/events`** before **`…/lights/{lightId}`** on models; scenes **`lights/events`** before **`GET …/lights`**); `lightstate.go`, `scenes.go`, `routines.go`, `routine_scheduler.go`; `backend/internal/store/routines.go` (**`TickRoutineRuns`** scene list for scheduler bumps); `backend/cmd/server/main.go` (shared **`RevisionHub`**); `web/app/models/detail/ModelDetailClient.tsx`, `web/app/scenes/detail/SceneDetailClient.tsx` (**`EventSource`** + delta merge/resync); `README.md` (REQ-029) | AC Feature "High-throughput light updates (REQ-029)" in **`docs/acceptance_criteria.md`**; **`TestModelLightsEvents_streamAfterPatch`**, **`TestSceneLightsEvents_streamAfterScenePatch`** (`internal/httpapi/lightevents_test.go`); **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req029 ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`** | Pass | 2026-04-06: **Audit vs `docs/architecture.md` §3.2 / §3.18 / §8.19:** **`text/event-stream`**, **`data:`** JSON **`{"seq":…,"deltas":[…]}`** after successful light-state commits; clients merge deltas incrementally per **REQ-041** and perform full resync only on sequence gaps or stream errors. **`main`** shares one hub with **`StartRoutineScheduler`** so tick-driven updates notify subscribers. Batch/bulk write paths unchanged from **TRC-030** / **TRC-053**. **Optional gap:** no **`TestAcceptance_REQ029_*`** in **`internal/acceptance`**. Verifier made **traceability-only** edits. **Updated 2026-05-12:** refreshed README/traceability wording for **REQ-041** delta payloads. |
| TRC-064 | REQ-022, REQ-024, REQ-025, REQ-027 — Python routine page: editor → API reference → unified run/viewport (§4.13 refresh) | `web/app/routines/python/PythonRoutineEditorClient.tsx` (**toolbar → editor → catalog → unified** **`targetSceneId`**); `web/components/PythonSceneApiCatalogSection.tsx` (**`<select>`** entry picker, **Put this example in your code**, **`#python-scene-api-catalog`**); `web/components/PythonCodeMirrorEditor.tsx` (**`editorViewRef`**); `web/lib/insertPythonEditorSnippet.ts` + **`.test.ts`** (caret vs EOF insert); `web/lib/pythonSceneApiCatalog.ts` (**commented** **`SCENE_API_CATALOG`** snippets + **`PYTHON_ROUTINE_DEFAULT_SOURCE`**); **`docs/architecture.md`** §4.13 / §8.18 | AC Features **Python scene routines (REQ-022)**, **Python routine API reference below editor (REQ-024)**, **default sphere template (REQ-025)**, **Routine authoring unified run-in-scene and live viewport (REQ-027)** in **`docs/acceptance_criteria.md`**; **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`**; **`TestAcceptance_REQ024_pythonRoutineApiReferenceBelowEditor`**; **`TestAcceptance_REQ027_unifiedRunAndViewportPythonAndShape`**; **`lib/pythonSceneApiCatalog.test.ts`**; **`lib/insertPythonEditorSnippet.test.ts`**; **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test && npm run build`** | Pass | 2026-04-06: **Architectural audit vs current §4.13:** DOM order matches **toolbar → CodeMirror → REQ-024 section → unified region** (one **Room** **`select`** for **Start**/**Stop** + **`SceneLightsCanvas`** + resets; no separate **Run on scene** vs **Visual debug**). **REQ-024:** picker + detail + **`#`** in every catalog snippet + insert via **`EditorView.dispatch`**. **REQ-025:** default template lines carry brief **`#`** comments. **§8.18** narrative: single scene id for run + viewport refresh. **Note:** **TRC-058** / **TRC-061** rows describe the **pre-refresh** bottom-catalog / dual-selector layout; **TRC-064** is the authoritative pass for **§4.13** **DOM** **order** **and** **REQ-024**/**027** **interaction** **after** **requirements**/**architecture**/**implementor** **updates**. **Updated 2026-04-06:** **`TestAcceptance_REQ024_*`** / **`TestAcceptance_REQ027_*`** in **`internal/acceptance`** close the prior **TRC-017** optional gap for those REQ blocks. Verifier made **traceability-only** edits. |
| TRC-065 | REQ-030 — Python scene binding: random hex colour helper | `web/public/dlm-python-scene-worker.mjs` (**`random_hex_colour`** → **`pyodide.runPython`** with **`random.randrange(0x1000000)`** and **`#%06x`**); `web/lib/pythonSceneApiCatalog.ts` (**`SCENE_API_CATALOG`** row + **`PYTHON_ROUTINE_DEFAULT_SOURCE`** uses **`scene.random_hex_colour()`**); `web/lib/pythonRoutineCodemirror.ts` (**`SCENE_API_COMPLETIONS`**); **`docs/architecture.md`** §3.17 / §4.13 / §8.17 | AC Feature **Python scene API random hex colour helper (REQ-030)** in **`docs/acceptance_criteria.md`**; **`pythonSceneApiCatalog.test.ts`**; **`pythonRoutineCodemirror.test.ts`**; **`TestAcceptance_REQ030_pythonSceneRandomHexColour`**; **`cd backend && go test ./... -count=1`**; **`go build ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`** | Pass | 2026-04-06: **Audit vs architecture:** **`scene.random_hex_colour()`** is **synchronous**, **no** **`fetch`** (**§3.17** **table** **HTTP** **=** **None**); **Pyodide** **Python** **`random`** **matches** **REQ-030** **rule** **2**. **Catalog** **+** **completions** **+** **default** **template** **aligned** (**REQ-024**/**REQ-025** **per** **§4.13**). **§8.17** **diagram** **unchanged** **(**local** **call** **only**)**. Verifier made **traceability-only** edits. |
| TRC-066 | REQ-031 — Skip redundant light-state work (visualization, in-memory authority, device traffic) | `backend/internal/store/lightstate_equiv.go`; `backend/internal/store/store.go` (`PatchLightState`, `BatchPatchLightStates`, `ResetAllLightStates` compare-before-`UPDATE`); `backend/internal/store/scenes.go` (`SceneBulkPatchResult.UpdatedCount` = rows written, `UnchangedAll`); `backend/internal/store/routines.go` (`TickRoutineRuns` / `applyRandomColourCycleAllTick` write-count gate); `backend/internal/httpapi/lightstate.go` (`unchanged` / `unchanged_all`, revision notify only on write); `backend/internal/httpapi/scenes.go` (`NotifyAfterSceneLightPatch` when `updated_count > 0`); `backend/internal/httpapi/revision_hub.go` (unchanged) | AC Feature **Redundant light-state skips (visualization, in-memory authority, device traffic) (REQ-031)** in **`docs/acceptance_criteria.md`**; `TestEquivLightStateTriple`, `TestStore_LightStateListPatch`, `TestStore_BatchPatchLightStates`, `TestStore_ResetAllLightStates_noopWhenAlreadyDefault`, `TestPatchSceneLightsSceneAndBatch` (idempotent second scene patch), `TestModelLightsEvents_noBumpWhenPatchUnchanged`; **`cd backend && go test ./... -count=1`** | Pass | 2026-04-07: **Audit vs `docs/architecture.md` §3.19:** equivalence triple + tolerance; transactional read-merge-compare; optional JSON flags; SSE/revision only after real writes; scene **`updated_count`** semantics; routine tick omits notification when zero SQLite writes. No application code changes by verifier. |
| TRC-067 | REQ-031 — Client three.js elision (signature-based effect deps) | `web/lib/lightStateSignature.ts` (`modelLightsVizSignature`, `sceneItemsVizSignature`); `web/components/ModelLightsCanvas.tsx`, `web/components/SceneLightsCanvas.tsx` (**`useEffect`** depends on **`vizSig`** per §3.19 / §4.7 / §4.9) | AC Feature **REQ-031** (skip unnecessary visualization rebuild when state equivalent); **`web/lib/lightStateSignature.test.ts`**; **`cd web && npm run lint && npm test -- --run`** | Pass | 2026-04-07: **Audit vs §3.19 / §4.7 / §4.9:** stable signature over sorted ids, positions, **`on`** / normalized hex / **`brightness_pct`**; canvases avoid full teardown when parent re-renders with referentially new arrays but identical effective viz state. ESLint **`exhaustive-deps`** suppressed with documented rationale. Verifier made **traceability-only** edits. |
| TRC-068 | REQ-032 — Novice Python sample routines (growing sphere, sweeping cuboid) | `web/lib/pythonRoutineSamples.ts` (**`PYTHON_SAMPLE_GROWING_SPHERE_SOURCE`**, **`PYTHON_SAMPLE_SWEEPING_CUBOID_SOURCE`**); `web/public/dlm-python-scene-worker.mjs` (**`scene.max_x`**, **`max_y`**, **`max_z`**); `web/lib/pythonSceneApiCatalog.ts` (**`PYTHON_SCENE_API_CATALOG_FULL`**, **`scene.max_*`** rows, sample entries import from **`pythonRoutineSamples.ts`**); `web/components/PythonSceneApiCatalogSection.tsx`; `web/app/routines/python/PythonRoutineEditorClient.tsx` (**Load … sample** buttons) | AC Feature **“Novice Python sample routines growing sphere and sweeping cuboid (REQ-032)”** in **`docs/acceptance_criteria.md`**; **`web/lib/pythonRoutineSamples.test.ts`**; **`web/lib/pythonSceneApiCatalog.test.ts`**; **`web/lib/pythonRoutineCodemirror.test.ts`**; **`TestAcceptance_REQ032_threeSeededPythonSampleRoutines`**; **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test -- --run && npm run build`** | Pass | 2026-04-07: **Audit vs `docs/architecture.md` §3.17 / §3.17.1 / §4.13:** single source file for sample strings; worker **`max_*`** from dimensions; samples use scene API only (**`set_lights_in_sphere`**, **`get_lights_within_cuboid`**, **`set_lights_in_cuboid`**, **`update_lights_batch`**, **`random_hex_colour`**); **`_grow`** / **`_sweep`** state across worker iterations preserves cooperative stop. **REQ-025** default unchanged. **Updated 2026-04-07:** **`TestAcceptance_REQ032_*`** closes prior **TRC-017** optional gap — **TRC-073** is authoritative for **three** shipped samples + Go seed. Verifier made **traceability-only** edits. |
| TRC-069 | REQ-032 — Architecture documents sample placement and REQ-025 relation | `docs/architecture.md` §3.17.1, §4.13 (REQ-032 bullets), §1 table, traceability appendix | AC scenario **“Architecture documents where REQ-032 samples live”** (parent **REQ-032**) | Pass | 2026-04-07: Architecture names **`web/lib/pythonRoutineSamples.ts`**, catalog + toolbar delivery, and **REQ-025** default template unchanged unless replaced later. Verifier made **traceability-only** edits. |
| TRC-070 | REQ-017 + REQ-032 — Factory reset and fresh DB re-seed three default Python routines | `backend/internal/store/store.go` (`SeedDefaultPythonRoutines`, `seedDefaultPythonRoutinesTx`, `FactoryReset`, `migrateLegacyRandomColourRoutines`); `backend/internal/seed/python_routine_samples_gen.go`; `scripts/gen-python-routine-samples-go.mjs`; `backend/cmd/server/main.go` (seed after models); `web/package.json` (`release:sync` invokes generator) | `TestStore_FactoryReset` (3 models + **3** routines); `TestStore_SeedDefaultPythonRoutines_idempotent`; `TestAPIv1FactoryReset_resetsToThreeSamples` (**GET `/api/v1/routines`**); AC Features **Options factory reset (REQ-017)** + **REQ-032** seeding (doc audit) | Pass | 2026-04-07: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-agent ./cmd/server`**; **`cd web && npm run lint && npm test -- --run && npm run build`**. **Audit vs `docs/architecture.md` §3.8.1 / §3.14 / §8.13:** one transaction deletes `routine_runs` + `routines` then scenes/models/lights, re-seeds **three** geometric models + **three** `python_scene_script` rows; empty `routines` on first start gets the same three samples; legacy `random_colour_cycle_all` rows **`UPDATE`** to Python with canonical third-sample source. **Supplements** **TRC-044** / **TRC-055** (factory reset) with **Python** seed path. Verifier made **traceability-only** edits. |
| TRC-071 | REQ-021 — Scene routines: persistence and start/stop only on server; **no** in-process Go routine ticker | `backend/internal/store/routines.go` (`StartRoutineRun` inserts `routine_runs` only); `backend/cmd/server/main.go` (**no** scheduler goroutine); **`routine_scheduler.go` removed**; `web/public/dlm-python-scene-worker.mjs`; **`web/components/ShapeAnimationRoutineHost.tsx`** (**`shape_animation`**); `web/app/scenes/detail/SceneDetailClient.tsx` | `TestRoutinesCreateStartStopDelete` (`internal/store/scenes_test.go` — expects running row, not server-lit lights); **`TestAcceptance_REQ021_sceneRoutinesPythonAndSceneAPI`**; `go test ./... -count=1`; AC Feature **Scene routines (REQ-021)** (doc audit vs §3.16–§3.17); **TRC-074** (**REQ-033** shape path) | Pass | 2026-04-07: Same regression commands as **TRC-070**. **Audit:** no **`time.Ticker`** / **`TickRoutineRuns`** / server-side colour-cycle ticks; light mutations from automation occur only via browser **Pyodide** (**Python**) or **`shapeAnimationEngine`** (**`shape_animation`**) + **`fetch`** to §3.15 routes. **REQ-029** SSE + revision hub unchanged for patches from any writer. **Updated 2026-04-07:** **`TestAcceptance_REQ021_*`** locks **`requirementBlock("REQ-021")`** + router/main/store/UI/worker/architecture cross-checks (**TRC-017**). **Supersedes** matrix references in **TRC-054**, **TRC-063**, **TRC-066** that listed **`routine_scheduler.go`** or **`TickRoutineRuns`** as active components — use **TRC-071** as authoritative for post-2026-04-07 code. Verifier **traceability-only**. |
| TRC-072 | REQ-023 — Create routine: choose Python or shape animation (no third creatable kind) | `web/app/routines/new/RoutineNewClient.tsx` (kind choice → `python_scene_script` or `shape_animation`); `web/app/routines/python/new/page.tsx` (**`redirect`** `/routines/new`); `web/lib/routines.ts` (`ROUTINE_TYPE_SHAPE_ANIMATION`, `isCreatableRoutineType`); `backend/internal/store/routines.go`; `backend/internal/httpapi/routines.go` | `web/lib/routines.test.ts`; `TestStore_CreateRoutine_rejectsRandomColourType`; AC Feature **Create routine type selection (REQ-023)** in **`docs/acceptance_criteria.md`**; **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test -- --run && npm run build`** | Pass | 2026-04-07: **Supersedes** prior **TRC-072** row that described **Python-only** create (**obsolete** after **REQ-033**). **Audit vs §4.12 / REQ-023:** two creatable kinds; legacy **`random_colour_cycle_all`** not creatable via **`POST`**. **TRC-057** remains historical for early **`<select>`** UI; current UI uses **kind** **radio** group. Verifier **traceability-only**. |
| TRC-073 | REQ-032 — Three full sample scripts (sphere, cuboid, random colour cycle) + TS→Go seed generation | `web/lib/pythonRoutineSamples.ts` (**`PYTHON_SAMPLE_*`** ×3); `scripts/gen-python-routine-samples-go.mjs`; `backend/internal/seed/python_routine_samples_gen.go`; `web/lib/pythonSceneApiCatalog.ts` (**third** **`kind: "sample"`** row); `web/app/routines/python/PythonRoutineEditorClient.tsx` (**Load random colour cycle sample**); `web/lib/pythonRoutineSamples.test.ts` | `pythonRoutineSamples.test.ts` (3 tests); `pythonSceneApiCatalog.test.ts` (**`sample-random-colour-cycle`** id); `TestStore_SeedDefaultPythonRoutines_idempotent`; **`TestAcceptance_REQ032_threeSeededPythonSampleRoutines`**; full backend + frontend regression | Pass | 2026-04-07: Same regression as **TRC-070**. **Audit vs §3.17.1:** random cycle uses **`set_all_lights`**, **`get_all_lights`**, **`update_lights_batch`**, **`scene.random_hex_colour()`**, ~**1 s** **`CYCLE_SEC`**; Go **`DefaultPythonRoutineRows`** matches TS bytes via generator. **Supersedes** **TRC-068** row scope where it named only two samples — **TRC-073** is authoritative for **three** shipped defaults + backend seed. **Updated 2026-04-07:** **`TestAcceptance_REQ032_*`** locks **`requirementBlock("REQ-032")`** + key file wiring (**TRC-017**). Verifier **traceability-only**. |
| TRC-074 | REQ-033 — Shape animation routines (declarative `definition_json`, client simulation, scene light painting) | `backend/internal/store/shape_animation_def.go` (`ValidateShapeAnimationDefinitionJSON`); `backend/internal/store/routines.go`; `backend/internal/store/store.go` (`ensureDefinitionJSONColumn`); `backend/internal/httpapi/routines.go`; `web/lib/shapeAnimationEngine.ts`; `web/lib/shapeAnimationDefault.ts`; `web/components/ShapeAnimationRoutineHost.tsx`; `web/app/routines/shape/*`; `web/lib/scenes.ts` (`fetchSceneDimensions`, `fetchSceneLightsFlat`, `patchSceneLightsStateBatch`); `web/app/routines/new/RoutineNewClient.tsx`; `web/app/routines/page.tsx`; `web/app/scenes/detail/SceneDetailClient.tsx` | **`TestAcceptance_REQ033_shapeAnimationRoutines`**; `TestRoutines_ShapeAnimationCreatePatch` (`internal/store/routines_python_test.go`); **`web/lib/shapeAnimationEngine.test.ts`**; AC Feature **Shape animation routines declarative authoring and run (REQ-033)** in **`docs/acceptance_criteria.md`**; **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test -- --run && npm run build`** | Pass | 2026-04-07: **Audit vs `docs/architecture.md` §3.16 / §3.17.2 / §4.12 / §4.14 / §8.21 / §8.22:** SQLite **`definition_json`** column migration; server validates JSON on **`POST`/`PATCH`**; browser tick loop writes via **`PATCH …/lights/state/batch`**; overlap precedence by lower **`shapes[]`** index; **no** Go simulation ticker. **Updated 2026-04-07:** **`TestAcceptance_REQ033_shapeAnimationRoutines`** locks **`requirementBlock("REQ-033")`** + key file wiring (**TRC-017**). Verifier **traceability-only**. |
| TRC-075 | REQ-039 — Authoritative in-memory per-light operational state (no durable SQLite triple for **on**/**color**/**brightness_pct**); restart re-derives defaults per model geometry | `backend/internal/lightstate` (`equiv.go`, `store.go`: per-model triples, `Patch`, `BatchPatch`, `ResetAll`, `SetTriple`); `backend/internal/store/store.go` (`SetLightState`, `LoadLightStateFromDB`, read paths merge LS + geometry from SQLite); `backend/internal/store/scenes.go` (scene bulk/region patches via **`lightstate`**); `backend/cmd/server/main.go` (**`lightstate.New`**, **`SetLightState`**, **`LoadLightStateFromDB`** after seed/migrate); `backend/internal/httpapi` (handlers unchanged surface; store backs responses from LS) | `go test ./internal/lightstate -count=1`; `go test ./internal/store -count=1`; `go test ./internal/httpapi -count=1`; `go test ./internal/acceptance -count=1` (**`TestAcceptance_REQ011_*`**, **`TestAcceptance_REQ014_*`** assert authoritative / **REQ-039** wording); `cd backend && go test ./... -count=1` | Pass | 2026-04-08: **`cd backend && go test ./... -count=1`** — all packages green. **Audit:** new light rows persist **x,y,z** only; operational triples live in **`internal/lightstate`**. **`FactoryReset`** path reloads LS from DB counts + defaults. **Supersedes** narrative in **TRC-026** / **TRC-030** that implied SQLite as source of truth for per-light operational fields — use **TRC-075** for **REQ-039** after this date. Verifier **traceability-only**. |
| TRC-076 | REQ-031 — Equivalence before patching authoritative in-memory state (server path) | `backend/internal/lightstate/equiv.go` (**`EquivLightStateTriple`**); `backend/internal/lightstate/store.go` (patch/batch/reset compare); `backend/internal/store/lightstate_equiv_test.go` (**`TestEquivLightStateTriple`** → **`lightstate`** package); `backend/internal/store/scenes.go` (**`lightstate.EquivLightStateTriple`** / **`SetTriple`**); `backend/internal/httpapi` revision / unchanged flags where store delegates to LS | Same regression as **TRC-075**; **`go test ./internal/store -run TestEquivLightStateTriple -count=1`**; **`TestModelLightsEvents_noBumpWhenPatchUnchanged`** (`lightevents_test.go`) | Pass | 2026-04-08: Equivalence implementation lives under **`internal/lightstate`** ( **`store/lightstate_equiv.go`** removed). **TRC-066** component column citing **`internal/store/lightstate_equiv.go`** is **obsolete** — **TRC-076** + **TRC-075** are authoritative for server-side REQ-031 + REQ-039 paths. Verifier **traceability-only**. |
| TRC-077 | REQ-021 + REQ-038 + REQ-040 — Server `routineengine`: supervised `python3` (loopback §3.15) + start/stop API; headless; browser observer-only | `backend/internal/routineengine/engine.go`, `loopback.go`, `bootstrap.py`; `backend/internal/httpapi/routines.go` (**`Engine.Start`/`Stop`**); `backend/internal/httpapi/router.go` (**`routineengine.New`**); `backend/cmd/server/main.go` (**`StopAllRunningRoutineRuns`**); `web/app/scenes/detail/SceneDetailClient.tsx`, `web/app/routines/python/PythonRoutineEditorClient.tsx` (no **`PythonRoutineHost`**) | `go test ./... -count=1`; **`TestAcceptance_REQ021_sceneRoutinesPythonAndSceneAPI`**; AC **REQ-021** / **REQ-038** (headless + observer); `cd web && npm run lint && npm test -- --run` | Pass | 2026-04-08: **`cd backend && go test ./... -count=1`**; **`go build ./cmd/server`**; **`cd web && npm run lint && npm test -- --run`** — Pass. **Supersedes** **TRC-071** narrative that light mutations came only from browser **Pyodide** — production Python runs in **`python3`** subprocess with embedded bootstrap; UI copy updated. **TRC-063** references to **`routine_scheduler.go`** / **`TickRoutineRuns`** remain obsolete. Verifier **traceability-only** for this row. |
| TRC-078 | REQ-033 — Shape animation production ticks on server (`shapeanim` sim + `PatchSceneLightsBatch`) | `backend/internal/routineengine/shapeanim/sim.go`; `backend/internal/routineengine/engine.go` (**`startShape`**); `web/app/routines/shape/ShapeRoutineEditorClient.tsx` (no **`ShapeAnimationRoutineHost`** for production); `web/lib/shapeAnimationEngine.ts` (ghost/editor parity) | `go test ./... -count=1`; **`TestAcceptance_REQ033_shapeAnimationRoutines`** (store/http + catalog; browser host assertion removed earlier); `npm run lint` | Pass | 2026-04-08: Go **`time.Ticker`** (**`time.Second/60`**) drives **`PatchSceneLightsBatch`** via **`shapeanim`** port of **`web/lib/shapeAnimationEngine.ts`**; **`npm run lint`** Pass. **Supersedes** **TRC-074** component list claiming browser **`fetch`** batch as the only production path. Verifier **traceability-only**. |
| TRC-079 | REQ-021 BR5 + **`docs/architecture.md` §3.2 / §3.16** — Second **`POST …/routines/…/start`** while the scene already has **`running`** (**same or different** routine) → **`409`** **`scene_routine_conflict`**; success → **`201`** only | `backend/internal/store/routines.go` (**`StartRoutineRun`**: any existing **`routine_runs.status=running`** for **`scene_id`** → **`SceneRoutineConflictError`**); `backend/internal/httpapi/routines.go` (**`postSceneRoutineStart`**: **`201`** on success, no **`200`** idempotent path); `web/lib/routines.ts` (**`startSceneRoutine`**: **`201`** only) | **`TestStartRoutineRun_secondStartSameRoutine_returnsConflict`** (`internal/store/scenes_test.go`); **`TestRoutinesCreateStartStopDelete`**; **`TestAcceptance_REQ021_sceneRoutinesPythonAndSceneAPI`**; **`cd backend && go test ./... -count=1`** | Pass | 2026-04-09: Full backend test suite; audit vs **`docs/requirements.md`** REQ-021 BR5 and architecture **§3.2** start row / **§3.16** concurrency (**no** duplicate-start **`200`**). **Supersedes** prior idempotent **“already running same pair”** behavior recorded historically under **TRC-054** / **TRC-077** narratives. Verifier **traceability-only**. |
| TRC-080 | REQ-027 — Reset scene lights stops active routine run on that scene before **`PATCH …/lights/state/scene`** | `web/app/routines/python/PythonRoutineEditorClient.tsx`; `web/app/routines/shape/ShapeRoutineEditorClient.tsx` (**`fetchSceneRoutineRuns`** + **`stopSceneRoutineRun`** then **`patchSceneLightsStateScene`**) | AC Feature **Routine authoring unified run-in-scene and live viewport (REQ-027)**; **`TestAcceptance_REQ027_unifiedRunAndViewportPythonAndShape`**; **`cd web && npm run build`** | Pass | 2026-04-09: Doc-to-code audit vs **`docs/requirements.md`** REQ-027 + **`docs/architecture.md` §4.13** / **§8.18**; static export build green. Verifier **traceability-only**. |
| TRC-081 | REQ-013 BR6 — Bulk apply: **≥ 2** selected lights → **`PATCH …/lights/state/batch`**; **1** selected light → **`PATCH …/lights/{id}/state`** | `web/app/models/detail/ModelDetailClient.tsx` (**`BulkLightStatePanel.apply`**) | **`TestAcceptance_REQ013_modelViewPaginationAndBulkSettings`**; **`cd web && npm test -- --run`** | Pass | 2026-04-09: Audit vs **`docs/requirements.md`** REQ-013 rule 6 + **`docs/architecture.md` §3.10**; Vitest suite green. Verifier **traceability-only**. |
| TRC-082 | REQ-042 — Routine run visibility and visualization resync on return | `web/lib/useSceneLightsSSE.ts`; `web/lib/sseUrl.ts`; `web/lib/routines.ts` (**`SceneRoutineConflictError`**, **`startSceneRoutine`** **409** **`error.details`**); `web/app/scenes/detail/SceneDetailClient.tsx`; `web/app/routines/python/PythonRoutineEditorClient.tsx`; `web/app/routines/shape/ShapeRoutineEditorClient.tsx`; `docs/architecture.md` **§4.16**, **§8.23** | AC Feature **Routine run visibility and visualization resync (REQ-042)** in **`docs/acceptance_criteria.md`**; **`web/lib/sseUrl.test.ts`**; **`web/lib/routines.test.ts`**; **`TestAcceptance_REQ029_architectureDocumentsThroughputAndSSE`** (**`SceneDetailClient.tsx`** **`EventSource`** + **`/lights/events`** traceability); **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req042 ./cmd/server`**; **`cd web && npm run lint && npm test -- --run && npm run build`** | Pass | 2026-04-19: **`@verifier`** audit vs **`docs/architecture.md` §4.16** / **§8.23** — **`GET …/routines/runs`**, mount **SSE** + **`sseUrl`** dev bypass, **409** conflict reconciliation; full regression green. Verifier **traceability-only** (**no** application source edits). |
| TRC-083 | REQ-034 — Faint axis-aligned boundary cuboid and configurable scene margin | `docs/requirements.md`; scene-view implementation pending | Pending — tracked by `prompt-07-boundary-cuboid-and-margin-m.md` | Pending | 2026-05-12: Placeholder row added so REQ-034 has explicit trace coverage; implementation/tests still pending for faint cuboid rendering plus persisted margin `m` default/edit flow. |
| TRC-084 | REQ-035 — Physical lighting devices (WLED registry) | `backend/internal/store/devices.go`; `backend/internal/httpapi/devices.go`; `backend/internal/httpapi/router.go` | `TestStore_DeviceCreateListAssignUnassignDelete`; `TestStore_FactoryReset_clearsDevices`; `TestAPIv1Devices_createListAssignUnassign`; `TestAPIv1FactoryReset_resetsToThreeSamples` | Partial | 2026-05-12: CRUD/list/delete and reset cleanup are implemented/tested for WLED registry metadata; remaining gaps include fuller device-management UI and hardware/integrator validation beyond stored metadata. |
| TRC-085 | REQ-036 — Exclusive one-to-one device-model assignment and per-light push | `backend/internal/store/devices.go`; `backend/internal/httpapi/devices.go`; `backend/internal/devices/pusher.go`; `backend/internal/devices/wled.go`; `backend/internal/httpapi/deps_notify.go` | `TestStore_DeviceCreateListAssignUnassignDelete`; `TestAPIv1Devices_createListAssignUnassign`; `TestAPIv1Devices_assignConflict_returns409`; light-state HTTP/store regression tests via pusher notification paths | Partial | 2026-05-12: Store/API enforce one device per model and one model per device; pusher/WLED translation paths exist for assigned models. Remaining gaps include fuller edit UI, direct pusher unit coverage, and hardware-level verification. |
| TRC-086 | REQ-037 — Device discovery | `backend/internal/httpapi/devices.go` (`postDevicesDiscover`) | `TestAPIv1Devices_discover_returns501` | Pending | 2026-05-12: Discovery endpoint is an explicit **501** stub in `internal/httpapi/devices.go`; manual device add remains the available path. |
| TRC-087 | REQ-041 — Server-push visualization updates with incremental client apply | `backend/internal/httpapi/revision_hub.go`; `backend/internal/httpapi/lightevents_test.go`; `web/lib/lightDeltas.ts`; `web/lib/useSceneLightsSSE.ts`; `web/app/scenes/detail/SceneDetailClient.tsx`; `web/app/models/detail/ModelDetailClient.tsx` | `TestModelLightsEvents_streamAfterPatch`; `TestSceneLightsEvents_streamAfterScenePatch`; `TestModelLightsEvents_noBumpWhenPatchUnchanged`; `web/lib/lightDeltas.test.ts`; `web/lib/sseUrl.test.ts` | Pass | 2026-05-12: SSE revision hub emits delta payloads after successful light-state writes; model/scene clients merge deltas incrementally and resync on gaps/errors. Supplements **TRC-063** / **TRC-082** with a dedicated REQ-041 row. |
| TRC-088 | REQ-040 — Routine stop completes within two seconds | `backend/internal/httpapi/routines.go`; `backend/internal/routineengine/engine.go`; `backend/internal/routineengine/bootstrap.py`; `backend/internal/routineengine/shapeanim/sim.go` | Routine start/stop coverage in `internal/httpapi/routines.go` tests and `go test ./...`; **TRC-077** routine engine regression | Pass | 2026-05-12: Dedicated row split out from **TRC-077**; stop path calls engine stop then persists stopped status, with supervised Python/shape workers expected to halt within the REQ-040 bound. |
| TRC-089 | REQ-043 — Cross-platform single Go executables (Windows, Linux, Linux ARM) | `.github/workflows/release.yml` (**cross-compile** **`dlm_linux_arm64`**, **`dlm_linux_amd64`**, **`dlm_windows_amd64.exe`**); **`docs/architecture.md`** §3.4 | YAML audit vs **`docs/architecture.md` §3.4** (**`GOOS`/`GOARCH`**, embed via **`npm run release:sync`** before **`go build`**); **`CGO_ENABLED=0`** matches pure-Go SQLite guidance | Pass | 2026-04-20: **`@verifier`** workflow review + architecture §3.4 table match; release job builds three targets into **`dist/`** with **`trimpath`** / **`-ldflags=-s -w`** per §3.4 examples. Verifier edited **`.github/workflows/ci.yml`** comment only (**non-functional**). |
| TRC-090 | REQ-044 — GitHub Actions CI (build + test gate) | `.github/workflows/ci.yml` (**jobs:** **Web lint and test**, **Backend tests**, **Build Go binary with embedded UI**); **`docs/advanced-setup.md`** §Continuous integration | AC Feature **Cross-platform release binaries** / integration with CI expectations in **`docs/acceptance_criteria.md`** (REQ-044 PR gate); **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test`** (same commands as CI) | Pass | 2026-04-20: Workflow triggers on **`pull_request`** + **`push`** **`main`**; frontend **`npm ci`/`lint`/`test`**; backend **`go test ./...`**; embed sanity **`release:sync`** + **`go build`**. Maintainer **branch protection** must require the three **`name:`** strings (**documented** in **`docs/advanced-setup.md`**). |
| TRC-091 | REQ-044 — GitHub Actions release (tag + downloadable binaries) | `.github/workflows/release.yml` (**`on.push.tags` `v*`**); **`softprops/action-gh-release@v2`** | AC Feature **GitHub Actions CI and release (REQ-044)** tagged-release scenario (doc audit); workflow lists three **`dist/`** assets matching §3.4 basenames | Pass | 2026-04-20: Tag **`v*`** triggers **`npm ci`** + **`release:sync`** then **`GOOS`/`GOARCH`** builds + **`generate_release_notes`**. Matches **`docs/architecture.md` §6.8**. |
| TRC-092 | REQ-045 — Deployment runtime prerequisites (Python for custom routines only) | **`README.md`** (**Python** / **`DLM_PYTHON3`**); **`docs/advanced-setup.md`** prerequisites; **`backend/internal/routineengine/engine.go`** (**`DLM_PYTHON3`**) | Doc audit vs REQ-045 + **`docs/architecture.md` §6.9**; README states operator path needs **no** Node/Go on deploy host when using release binary; **`go test ./...`** regression | Pass | 2026-04-20: README documents **`python3`** only for Python routines and **`DLM_PYTHON3`** pointer to advanced-setup; aligns with existing engine env var. |
| TRC-093 | REQ-046 — README: binary download, Raspberry Pi focus, systemd, updates | **`README.md`** (GitHub Releases table, **`systemd`** unit, update steps, developer **`run.sh`** section without internal **`REQ-*`** in hobbyist prose) | AC Feature **README operator download Pi service and updates (REQ-046)** (Gherkin doc audit); **`TestAcceptance_REQ008_singleCommandBuildAndRun`** still finds **`./scripts/run.sh`** in README | Pass | 2026-04-20: README leads with Releases + Pi **`dlm_linux_arm64`** / **`chmod +x`** / **`HTTP_LISTEN`** default **`:8080`** via advanced-setup pointer; **`systemd`** example + preserve **`data`** on update; integrator SSE section retains **`/lights/events`** (**REQ-029** acceptance coupling). |

**Verifier notes (architecture):** Repository matches §2 layout intent (`backend/cmd/server`, `internal/config`, `internal/httpapi`, `internal/webdist`; `web/` source-only). **§7 flowchart:** one Go process serves `/health`, `/api/v1/*`, and embedded static export. **§8:** initial load and `fetch /api/v1/status` paths satisfied. **§3.2 ordering:** API and `/health` registered before `/` static handler. **Boundaries:** `backend` does not import `web` TS; built assets land in `internal/webdist/dist` via `scripts/copy-web-export.mjs` aligning with §3.5. **SPA fallback (§3.2):** implemented in `staticExportHandler` without violating API precedence.

**Verifier notes (REQ-005–REQ-007, 2026-04-03):** **§2 / §3.1** — `internal/wiremodel` and `internal/store` present as specified. **§3.2** — Model routes match table (`GET/POST /models`, `GET/DELETE /models/{id}` on the `/api/v1` submux). **§3.3** — SQLite + transactional create; FK pragma via DSN. **§3.6** — CSV rules implemented in `wiremodel.ParseLightsCSV`. **§4.6** — Models UI uses list/new/detail with client `fetch`; detail URL is `/models/detail?id=<uuid>` for static export compatibility (acceptable deviation from literal `/models/[id]` path). **§9** — `MaxBytesReader` caps upload size on create. No architectural violations requiring implementor rework.

**Verifier notes (REQ-008–REQ-009, 2026-04-03, post–§3.8 face/surface-even samples):** **§3.7** — `scripts/run.sh` unchanged intent. **§3.8 / code** — `segmentCountForChordBand` + `subdivideEdge` / `subdivideGreatCircleArc` enforce **0.05–0.10 m** consecutive chords; **sphere**: **Fibonacci** seeds + greedy **nearest-neighbor** order + great-circle subdivision (**no** stacked latitude rings); **cone**: **`coneBaseSpiral`** on **z=0** + **`coneRingAtZ`** lateral rings + **ruling** to **apex** + **`trimConeChain`** to target **n**; **cube**: **six** **face serpentines** + **`appendCubeBridgePoints`** (axis-aligned **surface** legs, `pointOnCubeSurface` / `axisSegmentOnCubeSurface`). Tests: **TRC-022** / **TRC-025**; **README** default samples bullet still **5–10 cm** and **500–1000** lights. **TRC-024** locks requirements.md (+ README for REQ-008) wording.

**Verifier notes (REQ-010, 2026-04-03, re-verified after §4.7 expansion):** Matches **`docs/architecture.md` §4.6–§4.7, §8.4–§8.6**: detail route **`/models/detail?id=`** + **`dynamic(..., { ssr: false })`**; **`InstancedMesh`** + **`SphereGeometry(SPHERE_RADIUS_M=0.005)`** + white **`MeshStandardMaterial`**; ambient + directional light; **`LineSegments`** from **`buildWireSegmentPositions`** (ascending **`id`**, **`wireSegments.test.ts`**); **`LineBasicMaterial`** **`transparent`** / **`opacity`** ≈ **0.28**; framing uses **`boundingFromLights`** + **sphere margin**; **`OrbitControls`**; **`Raycaster`** → **`instanceId`** with **fixed** DOM tooltip (**id** + **x,y,z** to **4** decimals); **pointer** hover (**mouse** only, rAF-throttled); **tap** pick vs orbit via **≤10 px** movement; miss clears pin; **`ResizeObserver`** on canvas host; empty **`lights`** still initializes scene + overlay. Requirements text locked by **`TestAcceptance_REQ010_threeJsModelView`** (**TRC-024**).

**Verifier notes (REQ-011 / REQ-012, 2026-04-04):** **§3.2** — `GET /models/{id}/lights/state`, `GET /models/{id}/lights/{lightId}/state`, and `PATCH …/state` are registered **before** `GET /models/{id}` so `lights/state` is not parsed as a `lightId`. **§3.3 / §3.9** — `lights` carries **`"on"`** (quoted), **`color`**, **`brightness_pct`**; migration adds columns on legacy DBs; **`MaxBytesReader`** on **PATCH** (**8 KiB**). **§4.7** — **on** spheres use **`MeshBasicMaterial`** + **`instanceColor`** (acceptable per architecture’s MeshBasic option); **off** uses **wireframe** + **opacity** ~**0.36**; **OrbitControls** + **segments** preserved. **§8.7** — persistence in **Go**, client merges **200** JSON into **`ModelDetail`** so the canvas **`useEffect([lights])`** rebuilds without full page reload. **TRC-023** remains valid for **diameter 0.02 m (radius 0.01 m)** geometry, **consecutive** segments, and picking; appearance is now state-driven per **REQ-012**.

**Verifier notes (REQ-011 / REQ-012 acceptance tests, 2026-04-04):** **TRC-029** records **`TestAcceptance_REQ011_restLightStateAPI`** and **`TestAcceptance_REQ012_visualizationReflectsLightState`**, extending the **TRC-017** pattern to **`### REQ-011`** / **`### REQ-012`** in **`docs/requirements.md`**. **Updated 2026-04-06:** **TRC-009** and **TRC-017** titles now span **REQ-001–REQ-017** (rollup); per-REQ rows **TRC-018**–**TRC-033** and **TRC-043**–**TRC-044** remain authoritative. **REQ-013** automation is **TRC-031** / **TRC-033**. **REQ-016** / **REQ-017** use **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`** and **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`** (**TRC-043** / **TRC-044**). **REQ-015** has **`TestAcceptance_REQ015_scenesCompositeSpacePlacementCRUD`** recorded in **TRC-050**; **TRC-040** / **TRC-042** remain implementation/architecture gates. **Updated 2026-04-07:** **`TestAcceptance_REQ021_sceneRoutinesPythonAndSceneAPI`** and **`TestAcceptance_REQ032_threeSeededPythonSampleRoutines`** extend the same **TRC-017** **`requirementBlock`** pattern to **`### REQ-021`** and **`### REQ-032`** (**TRC-071** / **TRC-073**).

**Verifier notes (REQ-013, 2026-04-04):** **§3.2 / §3.10** — **`PATCH /api/v1/models/{id}/lights/state/batch`** is registered on the **`/api/v1`** submux immediately after **`GET …/lights/state`** and before **`…/lights/{lightId}/state`**, avoiding pattern shadowing. **Store** applies duplicate detection, range check via model **light count**, and **per-row** merge semantics consistent with **`PatchLightState`**. **§4.8** — UI uses **in-memory** paging over the single detail payload (**no** extra list endpoint), **three** page sizes **25 / 50 / 100**, and **bulk** **PATCH** for **REQ-011** fields. **Doc drift:** **`TestAcceptance_REQ013_modelViewPaginationAndBulkSettings`** is recorded under **TRC-031** (implementation + UI) and **TRC-033** (requirements-block automation, **TRC-029** pattern).

**Verifier notes (full re-pass, 2026-04-04):** **`@verifier`** re-run: architectural spot-check — no new violations vs **`docs/architecture.md` §3.2 / §3.10 / §4.7–§4.8 / §7–§8**; **`go test ./... -count=1`** and **`go build -o /tmp/dlm-verify-2 ./cmd/server`** (`backend/`); **`npm run test`** and **`npm run build`** (`web/`) — all **Pass**. **TRC-030**–**032** verification text amended with **Re-verified** lines; **TRC-033** added for REQ-013 **`requirementBlock`** automation.

**Verifier notes (REQ-012 on-material + orbit, 2026-04-04, TRC-034):** **`TestAcceptance_REQ012_visualizationReflectsLightState`** and **`lib/lightAppearance.test.ts`** still cover requirements text and linear brightness; WebGL appearance is **manual** per §4.7 testing note. Prior matrix text mentioning **`MeshStandardMaterial`** for **on** spheres (e.g. TRC-023 narrative) reflected an earlier sketch; current **`MeshBasicMaterial` + `instanceColor`** remains within §4.7’s allowed patterns.

**Verifier notes (REQ-010 / REQ-012 / REQ-014 pass, 2026-04-04):** Full **`@verifier`** re-run after implementor changes: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify ./cmd/server`**; **`cd web && npm run test && npm run lint && npm run build`** — all **Pass**. **§3.2:** **`POST …/lights/state/reset`** registered with **`GET …/lights/state`** and **`PATCH …/state/batch`** before **`…/lights/{lightId}/state`** and **`GET /models/{id}`**. **§3.9 / §3.11:** **`ResetAllLightStates`** + HTTP handler match architecture. **§4.7:** **2 cm** spheres (**`radius 0.01`**), wire + **off** spheres use **`#D0D0D0`** at **opacity 0.15** (**85%** transparent) per current **`docs/requirements.md`**. **TRC-035**–**TRC-039** record trace rows; older rows (**TRC-027**) may still mention **wireframe off** — treat **TRC-036**–**TRC-038** as superseding current geometry/default-state aspects.

**Verifier notes (REQ-015 + REQ-006 scene references, 2026-04-04):** Architectural audit vs **`docs/architecture.md` §3.12–§3.13, §4.9, §7–§8.10–§8.12**: scene REST surface and SQLite schema match the documented table; **`model_in_scenes`** and **`scene_last_model`** error envelopes use **`writeAPIErrorDetail`**; API routes remain on the **`/api/v1`** submux ahead of static handling. **§8.4** delete-model sequence extended with **409** branch in code and tests. **REQ-015 acceptance parity:** **`TestAcceptance_REQ015_scenesCompositeSpacePlacementCRUD`** is recorded in **TRC-050**; **TRC-040** integration tests and **TRC-042** architecture audit remain the implementation gates.

**Verifier notes (REQ-015 re-verify, 2026-04-04, server-computed create):** Full pass: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-verifier ./cmd/server`**; **`cd web && npm run lint && npm run test && npm run build`**. **TRC-040** row updated (replaces obsolete **`TestScenes_RejectNegativeContainment`** narrative — negative model coords are shifted by **`OffsetsForFirstSceneModel`** on create). **TRC-042** records explicit architecture doc alignment for auto placement + strict create JSON. **Boundaries:** verifier made **no** application source edits.

**Verifier notes (REQ-016 / REQ-017, 2026-04-04; acceptance tests 2026-04-06):** Full pass: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-pass ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`**. **§3.2:** **`POST /api/v1/system/factory-reset`** is on the **`/api/v1`** submux before catch-all **`GET /{path...}`**. **§3.14:** **`FactoryReset`** matches documented delete order + **`seedThreeCanonicalSamplesTx`**. **§4.10:** **`/options`** static route exported; confirmation before POST; redirect to **`/models`** with query then strip (**architecture default**). **§4.7 / §4.9:** camera reset via **`cameraResetVersion`** preserves **REQ-010**/**012** rendering while resetting orbit. **`internal/acceptance`:** **`TestAcceptance_REQ016_cameraResetModelAndSceneViews`** and **`TestAcceptance_REQ017_optionsFactoryResetWithConfirmation`** lock **`### REQ-016`** / **`### REQ-017`** text in **`docs/requirements.md`** (**TRC-043**, **TRC-044**; **TRC-009** / **TRC-017** rollups updated). **Boundaries:** traceability-only edits in this pass.

**Verifier notes (REQ-018, 2026-04-04):** **`@verifier`** pass recorded as **TRC-045**. Full regression: **`go test ./... -count=1`**; **`go build ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`**. **§4.11** satisfied by **`AppShell`** + **`layout`** theme script + **`Button`**/**`ButtonLink`** icon rule + left nav entries. **`TRC-009`**/**`TRC-017`** rollups in this file still list **REQ-001–REQ-017** in prose; **REQ-018** is covered by **TRC-045** until those summary rows are editorially refreshed.

**Verifier notes (REQ-018 default + REQ-019 viz, 2026-04-04):** **`@verifier`** re-run after implementor: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-20260404 ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`** — all **Pass**. **TRC-046** records **REQ-019** (**§4.7** / **§4.9** fixed **`#262626`** viewport). **TRC-047** records **REQ-018** rule 1 (**`prefers-color-scheme`** when **`dlm-theme`** unset). **TRC-045** narrative on “default light” and “canvases follow **`html.dark`**” is **obsolete** for those points; use **TRC-046** / **TRC-047**. **Optional:** add **`TestAcceptance_REQ019_*`** for **TRC-017** parity.

**Verifier notes (REQ-020 extension + REQ-021 + REQ-017 routine wipe, 2026-04-05):** Full pass: **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test && npm run build`**. **§3.2:** literal segments `routines`, `state/scene`, `state/batch`, `routines/runs`, `start`, `stop` registered before ambiguous `{id}` patterns where applicable. **§3.15–§3.16:** scene batch + whole-scene patch and routine lifecycle align with architecture; **§7** diagram note updated in code (`routines` + `routine_runs` in SQLite). **TRC-053**–**TRC-055** record trace rows; **TRC-048**/**TRC-052** remain valid for cuboid/sphere spatial API baseline.

**Verifier notes (REQ-022, 2026-04-05 re-verify):** Full regression **`go test ./... -count=1`**; **`npm run lint`**, **`npm test`**, **`npm run build`** — all **Pass**. **TRC-056** updated to **Pass** — **`T_force`** = **5000 ms**; **Pyodide** **`ast.parse`** diagnostics + **`scene.`** completions + **format-on-save** (**`black`** / fallback) match **§3.17** / **REQ-022** rules **3–4** at documented intent. **Manual / environment:** first **Pyodide** load and **micropip** **`black`** install require **network** and can take **tens of seconds** on slow links (architecture §6.2 trade-off). Verifier made **no** application source edits (traceability-only).

**Verifier notes (REQ-022 CodeMirror 6 mandate, 2026-04-05):** **`docs/requirements.md`** **REQ-022** **rule** **2** and **`docs/architecture.md`** **§3.17** / **§4.13** require **CodeMirror** **6** **only** for the **Python** **routine** **buffer**; implementation and **`web/package.json`** match (**no** **Monaco**). **`TestAcceptance_REQ022_pythonSceneRoutinesAndCodeMirror6`** + **`TRC-056`** / **`TRC-009`** / **`TRC-017`** updates record automation. Verifier made **traceability-only** edits.

**Verifier notes (REQ-023, 2026-04-05):** **`@verifier`** pass recorded as **TRC-057**. Full regression: **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test && npm run build`** — all **Pass**. No Mermaid boundary violations: create flow remains browser **`fetch`** → same-origin **`POST /api/v1/routines`** per §5. **TRC-009** / **TRC-017** rollup prose still lists **REQ-001–REQ-022** in some lines; **REQ-023** is covered by **TRC-057** until those summaries are editorially refreshed.

**Verifier notes (REQ-024–REQ-027, 2026-04-06):** **Superseded for §4.13 DOM order:** the first 2026-04-06 note assumed **toolbar → run → visual debug → editor → bottom catalog**. After **requirements**/**architecture**/**implementor** refresh, **`docs/architecture.md` §4.13** specifies **toolbar → editor → `#python-scene-api-catalog` → unified run/viewport**. **TRC-064** records verification of that layout and of **REQ-024**/**REQ-027**/**REQ-025** comment rules. **TRC-058**/**TRC-061** historical rows remain for file-path traceability; use **TRC-064** for current **§4.13** compliance. **Still valid from the original note:** **`docs/architecture.md` §3.17** worker **`scene`** API; **`iterationComplete`** refresh; **§3.15** **`PATCH …/lights/state/scene`**; **§4.7**/**§4.9** **`SceneLightsCanvas`**. **Updated 2026-04-06:** **`TestAcceptance_REQ024_pythonRoutineApiReferenceBelowEditor`** and **`TestAcceptance_REQ027_unifiedRunAndViewportPythonAndShape`** in **`internal/acceptance`** provide **`requirementBlock`** parity for **REQ-024**/**REQ-027** (**TRC-017** / **TRC-064**). **TRC-056** remains the primary gate for **REQ-022** core editor/worker + **CodeMirror** **6**.

**Verifier notes (REQ-028 emissive glow, 2026-04-06):** Full pass: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req028 ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`**. **Audit vs `docs/architecture.md` §4.7 / §6.5 / REQ-028:** **on** lights **`MeshStandardMaterial`** (**`metalness` 0**, **`roughness` 0.35**) with diffuse **`color`** from **`colorFromHexAndBrightness`**, **`emissive`** from full-chroma hex, **`emissiveIntensity`** from **`emissiveIntensityFromBrightness`** (monotonic **0–100%**, cap **2.2**); **off** lights unchanged **`MeshBasicMaterial`**; **`configureVizWebGLRenderer`** on both canvases. **TRC-062** is authoritative for **REQ-028**; older notes (**§4.7** **MeshBasic** **on**-spheres, **TRC-027** / **TRC-034** prose) are **obsolete** for **on**-sphere **material** class. **Optional:** add **`TestAcceptance_REQ028_*`** with **`requirementBlock("REQ-028")`** in **`internal/acceptance`** for **TRC-017** parity.

**Verifier notes (REQ-029 high-throughput lights + SSE, 2026-04-06):** **`@verifier`** pass recorded as **TRC-063**. Full regression: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req029 ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`** — all **Pass**. **§3.2** literal **`events`** segments precede **`lights/{lightId}`** / **`GET …/lights`** so routing matches architecture ordering note. **§3.18 / §8.19** SSE observer path implemented; integrator-facing batch routes remain **§3.10** / **§3.15** (prior trace rows). **Optional:** add **`TestAcceptance_REQ029_*`** with **`requirementBlock("REQ-029")`** for **TRC-017** parity.

**Verifier notes (REQ-030 random hex colour helper, 2026-04-06):** **`@verifier`** pass recorded as **TRC-065**. Full regression: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req030 ./cmd/server`**; **`cd web && npm run lint && npm test && npm run build`** — all **Pass**. **Audit vs `docs/architecture.md` §3.17 / §4.13 / §8.17:** **`dlm-python-scene-worker.mjs`** exposes **`random_hex_colour`** on the **`scene`** shim using **`pyodide.runPython('"#%06x" % __import__("random").randrange(0x1000000)')`** (**no** **HTTP**); **`pythonSceneApiCatalog.ts`** lists the method with **`#`** **comments** and default **`PYTHON_ROUTINE_DEFAULT_SOURCE`** uses **`scene.random_hex_colour()`**; **`pythonRoutineCodemirror`** completions include **`random_hex_colour`** (**`() → str`**). **`TestAcceptance_REQ030_pythonSceneRandomHexColour`** extends **TRC-017**/**TRC-009** rollups. Verifier made **traceability-only** edits.

**Verifier notes (REQ-031 redundant elision, 2026-04-07):** **`@verifier`** pass recorded as **TRC-066** / **TRC-067**. Full regression: **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test -- --run && npm run build`**. **Audit vs `docs/architecture.md` §3.19:** server-side equivalence before **`UPDATE`**; **`updated_count`** reflects SQLite writes on scene bulk; **`unchanged`** / **`unchanged_all`** on model endpoints; revision hub and scene patch notifications skipped when no rows change; routine scheduler does not append **`touched`** scenes when a tick performs zero writes. **Frontend:** **`vizSig`** decouples **`useEffect`** from referential **`lights`/`items`** churn. **Optional gap:** no **`TestAcceptance_REQ031_*`** in **`internal/acceptance/requirements_test.go`** — Gherkin in **`docs/acceptance_criteria.md`** is covered by **TRC-066**/**TRC-067** tests + doc review. **WLED** remains design-only in architecture (no code expected). Verifier made **traceability-only** edits (**`docs/traceability_matrix.md`**).

**Verifier notes (REQ-032 novice Python samples, 2026-04-07):** **`@verifier`** pass recorded as **TRC-068** / **TRC-069**. Full regression: **`cd backend && go test ./... -count=1`**; **`cd web && npm run lint && npm test -- --run && npm run build`**. **§4.13 extension:** **`PYTHON_SCENE_API_CATALOG_FULL`** appends two **`kind: "sample"`** entries; **TRC-064** remains authoritative for editor → catalog → unified viewport **DOM** order — REQ-032 adds sample rows + replace-all insert + toolbar loads. **§3.17:** **`scene.max_*`** getters match **`GET …/dimensions` → `max`** per architecture. **Timing:** sample scripts advance one logical step per worker **`runPythonAsync`** invocation; wall-clock **10 s** cycles align with **`time.monotonic()`** over many iterations (**§3.17** default **50 ms** gap). **Optional gap:** add **`TestAcceptance_REQ032_*`** with **`requirementBlock("REQ-032")`** for **TRC-017** parity (**@implementor**). Verifier made **traceability-only** edits.

**Verifier notes (Python-only routines, three seeded samples, no Go ticker — 2026-04-07):** Full pass: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-agent ./cmd/server`**; **`cd web && npm run lint && npm test -- --run && npm run build`**. **Architectural audit vs `docs/architecture.md`:** §3.8.1 / §3.14 / §8.13 satisfied by **`SeedDefaultPythonRoutines`** + **`FactoryReset`** seed + **`migrateLegacyRandomColourRoutines`**; **§3.16–§3.17 (2026-04-07 text):** absence of legacy **`StartRoutineScheduler`** / **`TickRoutineRuns`** / **`routine_scheduler.go`** remains true; **superseded 2026-04-08** for *where* production light mutations originate: **TRC-077** / **TRC-078** — **`internal/routineengine`** runs supervised **`python3`** and **`shapeanim`** ticks on the server; **TRC-071** / **TRC-074** **Pass** rationales that cited **only** browser **Pyodide** / **`fetch`** batch for production are **obsolete** (rows kept for path history). **§4.12 / REQ-023** satisfied by **two-kind** **`/routines/new`** (**Python** or **`shape_animation`**) and store **`CreateRoutine`** rules — **not** Python-only (**updated 2026-04-07**; see **TRC-072** / **TRC-074**). **Trace rows:** **TRC-070**–**TRC-074** plus **TRC-077**–**TRC-078**; older rows in **TRC-054**, **TRC-057**, **TRC-063**, **TRC-066**, **TRC-068** may still name **`routine_scheduler.go`** — treat **TRC-077**/**TRC-078** as authoritative for server engine paths. **Optional gap:** add **`TestAcceptance_REQ032_*`** for **TRC-017** parity where not already covered (**REQ-021** / **REQ-033** have acceptance tests). Verifier made **traceability-only** edits (**`docs/traceability_matrix.md`**).

**Verifier notes (REQ-039 in-memory authority + routine engine, 2026-04-08):** **`cd backend && go test ./... -count=1`** — Pass (**`internal/lightstate`** included). **`go build -o /tmp/dlm-verify ./cmd/server`** — Pass. **`cd web && npm run lint && npm test -- --run`** — Pass (**51** Vitest tests). **TRC-075** / **TRC-076** record **REQ-039** implementation and **REQ-031** equivalence relocation. **Server-side routine execution:** **TRC-077** / **TRC-078** record **`internal/routineengine`** (**`python3`** subprocess + **`shapeanim`**) aligned with **`docs/architecture.md`** loopback / headless routines; production scene light updates from routines are **no longer** browser-only. **Still outstanding vs full architecture (not covered by this pass):** **REQ-035**–**REQ-037** (device registry, WLED hardware client / push) — scaffold **`internal/devices`** may exist without integrator behavior; verify separately when **REQ-035**–**REQ-037** are in scope. Verifier made **traceability-only** edits (**`docs/traceability_matrix.md`**).

**Verifier notes (REQ-021 start concurrency + REQ-027 reset ordering + REQ-013 batch vs per-light, 2026-04-09):** Full regression: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-20260409 ./cmd/server`**; **`cd web && npm run lint && npm test -- --run && npm run build`** — all **Pass**. **TRC-079** records **REQ-021** BR5 + architecture **§3.16** (**no** second **`POST …/start`** while a run is active on the scene, including repeat start for the **same** routine; **`201`** only on success). **TRC-080** records **REQ-027** reset flow (**stop** then **whole-scene** default patch) on **Python** and **shape** authoring pages. **TRC-081** records **REQ-013** BR6 split (**batch** vs single **`PATCH …/{id}/state`**). Architectural **§7** boundary unchanged (single Go process, API before static). Verifier made **traceability-only** edits (**`docs/traceability_matrix.md`**).

**Verifier notes (REQ-042 run visibility + resync + dev SSE, 2026-04-19):** Full regression: **`cd backend && go test ./... -count=1`**; **`go build -o /tmp/dlm-verify-req042 ./cmd/server`**; **`cd web && npm run lint && npm test -- --run && npm run build`** — all **Pass**. **Audit vs `docs/architecture.md` §4.16 / §8.23 / §3.2:** **`POST …/start`** **409** **`scene_routine_conflict`** with **`error.details`** is handled in **`web/lib/routines.ts`**; **`useSceneLightsSSE`** centralizes **`EventSource`** on **`…/lights/events`** with **`eventSourceUrl`** (**`sseUrl.dev`** bypasses **`next`** rewrite buffering); **`SceneDetailClient`** documents **`EventSource`** + **`/lights/events`** for **REQ-029** acceptance (**`TestAcceptance_REQ029_architectureDocumentsThroughputAndSSE`**); scene **+** Python/shape pages refresh **`GET …/routines/runs`** with scene load where applicable. **TRC-082** records the row. **Optional gap:** no **`TestAcceptance_REQ042_*`** in **`internal/acceptance`** — gate is **TRC-082** + AC Gherkin + **`sseUrl.test.ts`** / **`routines.test.ts`** + full regression.

**Verifier notes (traceability cleanup, 2026-05-12):** Matrix-only cleanup pass. Edited **TRC-009**, **TRC-021**, **TRC-023**, **TRC-026**, **TRC-029**, **TRC-044**, and stale verifier notes to align defaults, 2 cm sphere wording, `run.sh` skip/force rules, factory-reset routine delete ordering, and the existing **`TestAcceptance_REQ015_scenesCompositeSpacePlacementCRUD`** row (**TRC-050**). Added **TRC-083**–**TRC-088** so **REQ-034**, **REQ-035**, **REQ-036**, **REQ-037**, **REQ-041**, and standalone **REQ-040** all have explicit trace rows. Template column shape preserved (`ID | Functional Requirement | System Component | Test Case Number | Status | Verification`). Sanity tests re-run for this verifier pass: **`cd backend && go test ./...`** and **`cd web && npm test`**.

**Next step:** Re-run **`@verifier`** after material code or requirement changes; use **`@implementor`** if a row fails or architecture shifts.
